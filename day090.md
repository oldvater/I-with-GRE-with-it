# 2026.3.25
效率好低啊，怎么现在就进入徘徊期了，麻了。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 7/8 | 1 | 🔄 挂起 | |
| 5 | 链表 | 14  | 1/14 | 13 | 开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 3)
- **目标单元**：[链表]
- **周期剩余**：6 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 1290.二进制链表转整数
  [https://leetcode.cn/problems/convert-binary-number-in-a-linked-list-to-integer]

## 思路：
- 遍历每个元素的时候，将之前存储的ans*2（视为左移）加上该节点的值，然后往后遍历，最后得到的就是答案。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def getDecimalValue(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: int
        """
        ans = 0
        node = head
        while node is not None:
            ans = ans*2 + node.val
            node = node.next
        return ans
```
- 2058.找出临界点之间的最小和最大距离
  [https://leetcode.cn/problems/find-the-minimum-and-maximum-number-of-nodes-between-critical-points]

## 思路：
- 定义first为出现极值点的第一个点，last为当前遍历出现极值点的最后一个点，当前最小距离minD，
  pre，cur，fut分别为前一节点，当前节点和后一节点，当出现极值点时，如果first=0，那么说明当前是第一个极值点，更新first为当前下标，
  如果last大于0，则说明last已至少进行过一次更新（因为初始last=0，而0不可能为极值点，它没有前面的元素），
  直接更新minD为minD和i-last的最小值，然后将i赋值给last，每次循环结束i需要加一，然后pre，cur，fut往后移动，直到fut.next为null。
  最后检查first是否等于last，如果等于，说明最多一个极值点，则返回[-1,-1]，否则返回[minD,last-first]。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def nodesBetweenCriticalPoints(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: List[int]
        """
        pre, cur, fut = head, head.next, head.next.next
        first, last, minD = 0, 0, float('inf')
        i = 1
        while cur.next is not None:
            curv = cur.val
            if (pre.val - curv)*(curv - fut.val) < 0:
                if first == 0:
                    first = i
                if last > 0:
                    minD = min(minD, i - last)
                last = i
            i += 1
            pre, cur, fut = cur, fut, fut.next
        return [minD, last-first] if last != first else [-1, -1]
```


## 考研每日总结
- 408：查找3.1，3.2。
- 数学：第八讲看一半了。

## 每日碎碎念
* 完蛋了，今天即使没有那么多事，甚至只有一节课，复习效果都这么差，效率太低了，我需要一场灵魂的放松。
* 发现了一个avl的邪修方法：直接中序遍历当前avl的所有节点构成一个有序数组，然后增加和删除就都在这个有序数组里进行，排完了以后，将
  新的数组再重构为平衡二叉树（用二分选根节点），这样时间复杂度只有O（n），但是代码实现要简单了不是一两点，到时候老头考这个直接填，
  别人还在写avl的构造方法（甚至还不一定能写出来，毕竟那个查找子孙节点的最大高度太神秘了），我已经直接拿10分走了，听懂掌声，优势在我，
  二分找到根节点，左右子树自己填。
* 明天想办法改善改善。
