# 2026.2.23
打了一天游戏，无事发生。

## 力扣每日一题:
64.最小路径和
  [https://leetcode.cn/problems/minimum-path-sum?envType=study-plan-v2&envId=top-100-liked]

## 思路：
标准的动态规划题，把递推公式列出来就结束了。
递归公式为：dfs（i, j） = min(dfs(i-1, j), dfs(i, j-1)) + grid[i][j]
只需要利用这个递推公式来做就行
```python
  class Solution(object):
    def minPathSum(self, grid):
        """
        :type grid: List[List[int]]
        :rtype: int
        """
        m = len(grid)
        n = len(grid[0])
        cache = [[-1] * n for _ in range(m)]
        def dfs(i, j):
            if i < 0 or j < 0:
                return float('inf')
            if i==0 and j==0:
                return grid[0][0]
            if cache[i][j] != -1:
                return cache[i][j]
            res = min(dfs(i-1, j), dfs(i, j-1)) + grid[i][j]
            cache[i][j] = res
            return res
        return dfs(m-1, n-1)
        
```

## 每日碎碎念
* 还是给瓦氪金了，买了把混沌ak，ε=(´ο｀*)))唉。
* 明天赶进度把，今天本来计划写半章的也没写，布豪。
