# 2026.3.12
wc，这备考的暂时没啥压力，挺好的。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 2/6 | 4 | 开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[二分查找]
- **周期剩余**：4 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 1285.两个数组间的距离值
  [https://leetcode.cn/problems/find-the-distance-value-between-two-arrays]

## 思路：
- 先将两个数组分别排序，对于每个arr1中的值x，维护arr2中大于等于x-d的最小的值的下标j，这样在arr1后续的x值中，
  大于等于x-d的下标只能出现在i的右侧，然后，如果这时候j=len（arr2）（说明arr2中的所有数都小于x-d，即所有数都满足|x-arr2|>d）
  或者arr2[j]>x+d（即arr2中大于等于x-d的所有数都大于x+d，那么同样满足|x-arr2|>d）,ans+1，最后返回ans即可。
```python
  class Solution(object):
    def findTheDistanceValue(self, arr1, arr2, d):
        """
        :type arr1: List[int]
        :type arr2: List[int]
        :type d: int
        :rtype: int
        """
        arr1.sort()
        arr2.sort()
        ans = j = 0
        for x in arr1:
            while j < len(arr2) and arr2[j] < x - d:
                j += 1
            if j == len(arr2) or arr2[j] > x + d:
                ans += 1
        return ans
        
```
- 2389.和有限的最长子序列
  [https://leetcode.cn/problems/longest-subsequence-with-limited-sum]

## 思路：
- 看到子序列之和的最大长度，首先要想到将nums中的数字排序，这样前缀和对应的下标刚好是同相加数量里结果最小的（也即相同数量中所需元素最多的），
  那么只需要找到queries[i]在前缀和数组nums中对应的位置即可。
```python
  class Solution(object):
    def findway(self, nums, target):
        n = len(nums)
        left, right = -1, n
        while left + 1 < right:
            mid = (left + right)//2
            if nums[mid] > target:
                right = mid
            else:
                left = mid
        return right
    def answerQueries(self, nums, queries):
        """
        :type nums: List[int]
        :type queries: List[int]
        :rtype: List[int]
        """
        nums.sort()
        for i in range(1, len(nums)):
            nums[i] += nums[i-1]
        for i, q in enumerate(queries):
            queries[i] = self.findway(nums, q)
        return queries
```
- 1170.比较字符串最小字母出现频次
  [https://leetcode.cn/problems/compare-strings-by-frequency-of-the-smallest-character]

## 思路：
- 额外写两个函数，一个实现题干中的f（x）函数find（），维护一个最小字符，当出现更小字符时，替换，如果出现相同就频次加一
  另一个实现查找大于target的最小值所在的位置find_left（）。首先对words进行find（）处理，得到新的记录了最小频次的新words，对新words进行排序，
  这样对于queries里的值x，对其进行同样的find（）处理后得到对应字符串的最小字母频数find（x），然后在words中找到大于find（x）的最小数的位置，
  那么得到的下标i表示f(queries[i]) >= f(W)的数目，用words的长度减去其即可。
```python
  class Solution(object):
    def find(self, s):
        ans = 0
        small = 'z'
        for x in s:
            if x == small:
                ans += 1
            elif x < small:
                ans = 1
                small = x
        return ans
    def find_left(self, nums, target):
        n = len(nums)
        left, right = -1, n
        while left + 1 < right:
            mid = (left + right)//2
            if nums[mid] > target:
                right = mid
            else:
                left = mid
        return right
    def numSmallerByFrequency(self, queries, words):
        """
        :type queries: List[str]
        :type words: List[str]
        :rtype: List[int]
        """
        for i, s in enumerate(words):
            words[i] = self.find(s)
        words.sort()
        n = len(words)
        for i, s in enumerate(queries):
            queries[i] = n - self.find_left(words, self.find(s))
        return queries
```
## 考研每日进度
- 408：第三章完毕
- 数学：第二讲除了1000题的部分。

## 每日碎碎念
* wc，感觉学的不累，挺舒服的。
* 关于压缩数组的计算，有一个简单的方法，只需要记住对应的自变量，系数可记可不记，常数不记，
  然后拿a[0][0],a[0][1]这些容易直到的来待定系数反推即可，举个例子，对于三对角矩阵，只需要记2*i+j，
  最后的c根据a[0][0]（或[1][1]）对应的值计算出等于几即可。
* wc，第二讲好难啊，主要方法还是海涅定理、夹逼定理来做，还是得多做题，多积累。
* 还有一种同构的思想，对于一个e^x导致的无穷大，最好可以构建出分子分母同时包含e^x,从而通过洛必达消去e^x项。
