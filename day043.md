# 2026.2.3
早睡早起，规律生活，猪脚好吃。

## 力扣每日一题:
  84.柱状图中最大的矩形
  [https://leetcode.cn/problems/largest-rectangle-in-histogram?envType=study-plan-v2&envId=top-100-liked]

### 思路：
记录lefts和rights数组为垓下标在heights数组中左边/右边最近小于其高度的下标，左边如果不存在小于的则为-1，右边不存在小于
则为n，那么下标i对应的面积就是heights[i]*(rights[i] - lefts[i]),更新最大值即可，lefts和rights使用单调栈记录，
当栈顶对应的高度大于等于栈顶对应的高度时，弹出栈顶，直到栈顶元素首次小于于遍历元素。
- 三次递归：用三次遍历一次完成rights、lefts和ans的计算。
  ```python
  class Solution(object):
    def largestRectangleArea(self, heights):
        """
        :type heights: List[int]
        :rtype: int
        """
        n = len(heights)
        stack = []
        lefts = [-1]*n
        rights = [n]*n
        ans = 0
        
        for i in range(n - 1, -1, -1):
            while stack and heights[i] <= heights[stack[-1]]:
                stack.pop()
            if stack:
                rights[i] = stack[-1]
            stack.append(i)
        
        stack = []
        for i in range(n):
            while stack and heights[i] <= heights[stack[-1]]:
                stack.pop()
            if stack:
                lefts[i] = stack[-1]
            stack.append(i)
        
        for i in range(n):
            s = heights[i] * (rights[i] - lefts[i] - 1)
            ans = max(s, ans)

        return ans

- 两次遍历：可以合并rights和lefts的书写过程，首先修改rights的定义为：首个小于等于当前高度的下标
  （这样虽然可能会在存在相等heights时修改rights的值，导致前面的s变小，但后面的s依旧不变，因为lefts没变化），
  在这种情况下，可以记弹出的元素为j，由于j在遇到i才被弹出，之前一直不存在满足小于等于条件的下标，
  那么，i就是j右侧第一个小于等于j高度的下标（heights[i]<=heights[stack[-1]]）,更新rights[j]=i。
  ```python
  class Solution(object):
    def largestRectangleArea(self, heights):
        """
        :type heights: List[int]
        :rtype: int
        """
        n = len(heights)
        stack = []
        lefts = [-1]*n
        rights = [n]*n
        ans = 0
        
        for i in range(n):
            while stack and heights[i] <= heights[stack[-1]]:
                rights[stack.pop()] = i
            if stack:
                lefts[i] = stack[-1]
            stack.append(i)
        
        for i in range(n):
            s = heights[i] * (rights[i] - lefts[i] - 1)
            ans = max(s, ans)

        return ans

- 一次遍历：吓哭了，在两次遍历中我们已经能稳定的求出rights了，那么怎么同时获取到高度和lefts呢？
  ***注意到***：在两次遍历中，弹出的元素j，我们存储了rights[j] = i,以及知道heights[j]，那么lefts[j]呢？
  由于栈是单调栈，所有大于等于heights[j]的元素都不会被存储，所以j的下面也就是弹出j后的新栈顶就是j的lefts
  更新ans即可（可以加入哨兵-1，防止第一个元素没有没有下面的元素，无法计算面积，栈底加入了-1，最后就同样需要-1来把
  最后一个元素的s计算出来，加在heights末尾）。
  ```python
  class Solution(object):
    def largestRectangleArea(self, heights):
        """
        :type heights: List[int]
        :rtype: int
        """
        heights.append(-1)
        st = [-1]
        ans = 0
        for right, h in enumerate(heights):
            while len(st) > 1 and heights[st[-1]] >= h:
                i = st.pop()
                left = st[-1]
                ans = max(ans, heights[i] * (right - left - 1))
            st.append(right)

        return ans

## 每日碎碎念
* 今天早起，精神，看完第三章了，三天肝完这一块。
* 猪脚好吃捏。
