# 2026.1.18
依旧爽玩这一块。

## 力扣每日一题:
  17.电话号码的字母组合
  [https://leetcode.cn/problems/letter-combinations-of-a-phone-number?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 常规的在每一层进行选择，记得选择完以后恢复现场。
  ```python
  class Solution(object):
    def letterCombinations(self, digits):
        """
        :type digits: str
        :rtype: List[str]
        """
        ans = []
        tmp = []
        n = len(digits)
        d_to_c = [[] for _ in range(8)]
        d_to_c[0] = ['a', 'b', 'c']
        d_to_c[1] = ['d', 'e', 'f']
        d_to_c[2] = ['g', 'h', 'i']
        d_to_c[3] = ['j', 'k', 'l']
        d_to_c[4] = ['m', 'n', 'o']
        d_to_c[5] = ['p', 'q', 'r', 's']
        d_to_c[6] = ['t', 'u', 'v']
        d_to_c[7] = ['w', 'x', 'y', 'z']
        def dfs(num):
            if num == n:
                ans.append(''.join(tmp))
                return
            i = int(digits[num]) - 2
            for j in d_to_c[i]:
                tmp.append(j)
                dfs(num + 1)
                tmp.pop()
        dfs(0)
        return ans
- 由于这一题输出到ans的tmp长度固定，因此可以直接令tmp为长度固定的列表，每一次选择只需要修改tmp对应的层值就行，
  免去了恢复现场的步骤。
  ```python
  class Solution(object):
    def letterCombinations(self, digits):
        """
        :type digits: str
        :rtype: List[str]
        """
        ans = []
        n = len(digits)
        tmp = [0] * n                      ##这里把tmp设定为长度为n的列表
        d_to_c = [[] for _ in range(8)]
        d_to_c[0] = ['a', 'b', 'c']
        d_to_c[1] = ['d', 'e', 'f']
        d_to_c[2] = ['g', 'h', 'i']
        d_to_c[3] = ['j', 'k', 'l']
        d_to_c[4] = ['m', 'n', 'o']
        d_to_c[5] = ['p', 'q', 'r', 's']
        d_to_c[6] = ['t', 'u', 'v']
        d_to_c[7] = ['w', 'x', 'y', 'z']
        def dfs(num):
            if num == n:
                ans.append(''.join(tmp))
                return
            i = int(digits[num]) - 2
            for j in d_to_c[i]:
                tmp[num] = j              ##只需更新tmp对应层的值即可
                dfs(num + 1)
        dfs(0)
        return ans

## 每日碎碎念
* 今天陪别人过生日，吃爽了。
