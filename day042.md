# 2026.2.2
两点睡，八点起，阎王夸我好身体，今天一天没精神，效率低下，还是得早睡早起。

## 力扣每日一题:
  739.每日温度
  [https://leetcode.cn/problems/daily-temperatures?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 反向遍历：设置单调栈储存下标与对应长度的全0数组ans，对于第i天的温度t，如果栈中存在对应温度比t小的下标，弹出，直到没有比t小的为止
  （相等也弹出，因为是从后往前遍历，先存的也就是后面的更不重要），然后如果栈里存了值（即存在比t大的数），
  就把ans[i]改为stack[-1]-i(因为反向遍历，后面的先入栈)，然后把i存入stack中。
  ```python
  class Solution(object):
    def dailyTemperatures(self, temperatures):
        """
        :type temperatures: List[int]
        :rtype: List[int]
        """
        stack = []
        k = 1
        n = len(temperatures)
        ans = [0]*n
        for i in range(n-1, -1, -1):
            while stack and temperatures[i] >= temperatures[stack[-1]]:
                stack.pop()
            if stack:
                ans[i] = stack[-1] - i
            stack.append(i)
        return ans
- 正序遍历：创建stack，存还没有较大值的数对应的下标，这样，对于当前下标i和对应温度t，如果t＞栈顶对应温度，
  弹出栈顶值j，把i-j写入ans[j]直到t＜栈顶对应温度，然后把i压入栈。
  ```python
  class Solution(object):
    def dailyTemperatures(self, temperatures):
        """
        :type temperatures: List[int]
        :rtype: List[int]
        """
        stack = []
        k = 1
        n = len(temperatures)
        ans = [0]*n
        for i, t in enumerate(temperatures):
            while stack and t > temperatures[stack[-1]]:
                j = stack.pop()
                ans[j] = i - j
            stack.append(i)
        return ans


## 每日碎碎念
* 还是得早点睡，睡眠不足今天一天没精神。
* 两天一章这一块。
