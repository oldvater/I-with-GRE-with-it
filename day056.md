# 2026.2.17
新年快乐！
草，大年初一遇到一个sb，晦气。

## 力扣每日一题:
300.最长递增子序列
  [https://leetcode.cn/problems/longest-increasing-subsequence?envType=study-plan-v2&envId=top-100-liked]

## 思路：
用回溯去做的，还没有优化到最好，只有n方。
本质是对于一个下标i，往前遍历下标j，如果nums[j]<nums[i]，说明ji可以构成递增子序列，就将dfs（j）的结果和答案比大小，
最大值加一即可，最后，在主函数里，对每个下标都应用一次判断函数，找出最大值即可。
```python
  class Solution(object):
    def lengthOfLIS(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        cache = [-1] * (n+1)

        def dfs(i):
            res = 0
            if cache[i] != -1:
                return cache[i]
            for j in range(i):
                if nums[j] < nums[i]:
                    res = max(res, dfs(j))
            cache[i] = res + 1
            return res + 1
        
        ans = 0
        for i in range(n):
            ans = max(ans, dfs(i))
        return ans
        
```


## 每日碎碎念
* 大年初一，遇到精神病，搞得鸡飞狗跳，真晦气。
* 难受，好气啊。
* 没办法，不能因为个别人坏了新春的福气。
* 新年快乐
