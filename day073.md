# 2026.3.7
今天算是正式开始复习考研了，有一说一，感觉还行？数学有点无从下手。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 3/6 | 3 | 开始 | |
| 3 | 二分查找 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[滑动窗口与双指针]
- **周期剩余**：3 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2200.找出数组中的所有K近邻下标
  [https://leetcode.cn/problems/find-all-k-distant-indices-in-an-array]

## 思路：
- 有一说一，这题我现在还是云里雾里的，题解的想法是记录从0开始到k-1中的前k个数中等于key的最后出现的下标（也就是0的右k近邻）。
  然后依次遍历，对于一个新下标，首先更新为key的最远下标，那么可知这个下标一定在右k近邻中，此时，如果大于i-k也就是左k近邻
  ，那么就可以认为key在i的k近邻中，把i加入ans
```python
  class Solution(object):
    def findKDistantIndices(self, nums, key, k):
        """
        :type nums: List[int]
        :type key: int
        :type k: int
        :rtype: List[int]
        """
        last = float('-inf')
        for i in range(k-1, -1, -1):
            if nums[i] == key:
                last = i
                break
        
        ans = []
        n = len(nums)
        for i in range(n):
            if i + k < n and nums[i + k] == key:
                last = i + k
            if last >= i - k:
                ans.append(i)
        return ans
```
- 2269.找到一个数的K美丽值
  [https://leetcode.cn/problems/find-the-k-beauty-of-a-number]

## 思路：
- 本质是创建一个长度为k的滑动窗口x，然后如果num%x == 0的话，就是美丽数，count + 1，记得做数据类型的转换，初始的num是int，记得转成str。
```python
  class Solution(object):
    def divisorSubstrings(self, num, k):
        """
        :type num: int
        :type k: int
        :rtype: int
        """
        s = ""
        ans = 0
        num = str(num)
        for i, x in enumerate(num):
            s += x

            left = i - k + 1
            if left < 0:
                continue
            if int(s) != 0:
                if int(num) % int(s) == 0:
                    ans += 1
            
            s = s[1:]
        
        return ans

```
- 1461.检查一个字符串是否包含所有长度为k的二进制子串
  [https://leetcode.cn/problems/check-if-a-string-contains-all-binary-codes-of-size-k]

## 思路：
- 只需要建立一个字典，然后将字符串中所有出现过的长度为K的子串加入字典中，最后判断字典长度即可。
  本质就是一个长度为k的滑动窗口，然后判断是否在字典中，没有就加入，最后，如果字典的长度为2^k，那么说明包含所有长度为k的子串。
```python
  class Solution(object):
    def hasAllCodes(self, s, k):
        """
        :type s: str
        :type k: int
        :rtype: bool
        """
        dict1 = {}
        count = 0
        s1 = ''
        for i, x in enumerate(s):
            s1 += x
            
            left = i - k + 1
            if left < 0:
                continue
            
            if s1 not in dict1:
                dict1[s1] = 0
                count += 1
            
            s1 = s1[1:]
        
        if count == 2 ** k:
            return True
        return False

```
## 考研每日进度
- 408：数据结构第一章
- 数学：还没开始。

## 每日碎碎念
* 开学！
* 数学有一说一，挺难开始的，开始不知道看什么，云里雾里的感觉，明天再学吧。
