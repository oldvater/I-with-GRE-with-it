# 2026.2.22
两天一章，难道我真的是天才？

## 力扣每日一题:
62.不同路径
  [https://leetcode.cn/problems/unique-paths?envType=study-plan-v2&envId=top-100-liked]

## 思路：
孩子们。高考数学的来了，不过我们暂时不用数学方法，就用计算机嗯算。
- 递归公式为：dfs（i, j） = dfs(i-1, j) + dfs(i, j-1)
只需要利用这个递推公式来做就行
```python
  class Solution(object):
    def uniquePaths(self, m, n):
        """
        :type m: int
        :type n: int
        :rtype: int
        """
        cache = [[-1] * n for _ in range(m)]
        cache[0][0] = 1
        def dfs(i, j):
            if i < 0 or j < 0:
                return 0
            if cache[i][j] != -1:
                return cache[i][j]
            res = dfs(i-1,j) + dfs(i, j-1)
            cache[i][j] = res
            return res
        return dfs(m-1, n-1)
        
```
- 数学做法：我们可以观察到，从（0,0）到（m，n），如果限制只能向右和下走的话，往右走和往下走的次数是固定的，
  只不过是排列组合而已，即，想走到（m，n），需要m-1次向下，n-1次向右，总次数是m+n-2，
  只需要在m+n-2中确定m-1（n-1也行，为了方便，选择里面最小的）的位置即可。
```python
  def comb(n, k):
    k = min(k, n - k)
    res = 1
    for i in range(1, k+1):
        res = res * (n+1-i)//i
    return res

class Solution(object):
    def uniquePaths(self, m, n):
        """
        :type m: int
        :type n: int
        :rtype: int
        """
        return comb(m+n-2, m-1)
        
```



## 每日碎碎念
* 孩子们，两天看一张，还剩4章，来得及。
* hot100一刷也只剩9题了，爽！
