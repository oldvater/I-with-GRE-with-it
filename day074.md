# 2026.3.8
wc，这个408和数一好勾八难学啊，学到崩溃了才只学了一点点。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 4/6 | 2 | 开始 | |
| 3 | 二分查找 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[滑动窗口与双指针]
- **周期剩余**：2 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 3090.每个字符最多出现两次的最长子字符串
  [https://leetcode.cn/problems/maximum-length-substring-with-two-occurrences]

## 思路：
- 创建一个列表，记录字符出现的次数。对于一个窗口，如果一个字符出现的频率大于2，那么移动左端直到该字符的频率小于等于2，
  然后维护最大值和窗口长度即可。
  总之，对于一个不定长滑动窗口，左端口更新的规则从窗口长度达标变成了其他条件，比如这题的字符出现频率。
```python
  class Solution(object):
    def findKDistantIndices(self, nums, key, k):
        """
        :type nums: List[int]
        :type key: int
        :type k: int
        :rtype: List[int]
        """
        last = float('-inf')
        for i in range(k-1, -1, -1):
            if nums[i] == key:
                last = i
                break
        
        ans = []
        n = len(nums)
        for i in range(n):
            if i + k < n and nums[i + k] == key:
                last = i + k
            if last >= i - k:
                ans.append(i)
        return ans
```
- 1493.删掉一个元素以后全为1的最长子数组
  [https://leetcode.cn/problems/longest-subarray-of-1s-after-deleting-one-element]

## 思路：
- 设立一个count，用来记录0出现的次数，如果大于1，那么就移动左端，直到回到1，然后维护长度的最大值即可
  （由于必须要删除一个元素，如果left对应的是0的话删去left即可，如果不是的话那么就是全1，也要删去一个，不如删去left，
  因此最终答案是比窗口长度小1的）维护最大值即可。
  对于这种滑动窗口，最好是每次更新的时候都维护一次，防止因为某些条件导致的一直不维护，就像这题，
  如果更新条件设置为count>1才更新的话，遇到全1数组，直到迭代完毕都无法更新。
```python
  class Solution(object):
    def longestSubarray(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        ans = 0
        count = 0
        left = mid = 0
        z = False
        for i, x in enumerate(nums):
            count += 1 - x
            if count > 1:
                count -= 1 - nums[left]
                left += 1
            ans = max(ans, i - left)
        return ans
            
        
```
- 3634.使数组平衡的最少移除数目
  [https://leetcode.cn/problems/minimum-removals-to-balance-array]

## 思路：
- 同时维护左右两端，最少移除数目实际上就是求最长平衡子数组长度。
  先对数组排序，这样左边一定是子数组的最小值，右边一定是子数组的最大值
  左右端同时从0开始，如果nums[right] <= nums[left]*k,说明平衡数组可以扩张，直到nums[right] > nums[left]*k时，
  说明子数组不平衡了，需要移动left，使子数组的最小值增大，然后重新计算，每次都维护平衡数组长度的最大值
  （由于left更新时right是不满足平衡条件的，因此维护的长度为right-left）。
```python
  class Solution(object):
    def minRemoval(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
        nums.sort()
        cnt0 = left = right = 0
        n = len(nums)
        while right < n:
            while right < n and nums[right] <= nums[left] * k:
                right += 1
                cnt0 = max(cnt0, right - left)
            left += 1
        return n - cnt0

```
## 考研每日进度
- 408：数据结构第二章：有序表、单链表
- 数学：第一讲前两小节。

## 每日碎碎念
* wc，好多啊，感觉学不完了，焦虑ing。
* 我***的11408,408就很多了，数一更多，气笑了。
* 看看在6月之前能不能把基础都结束吧，希望可以。
* 学的好累啊，每天要学数学，408，还得去健，慢慢来吧，加油思密达。
