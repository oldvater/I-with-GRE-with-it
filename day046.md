# 2026.2.6
状态不对，明天得早起调整一下学习状态。

## 力扣每日一题:
295.数据流的中位数
  [https://leetcode.cn/problems/find-median-from-data-stream?envType=study-plan-v2&envId=top-100-liked]

## 思路：
在**leetcode4.寻找两个正序数组的中位数**中，我们已经得到了代码中描写中位数的最佳方法：维护两个区域，保证两个区域的
元素数量相差不超过1并且左边的区域规模大于右边区域的规模，右边最小元素大于左边最大元素，
这样中位数就是左边数组的最大值（左数组大于右数组）与左数组最大值和右数组最小值的平均值（左右规模相同），
在本题中，我们可以建立两个堆来保证快速找到最小值和最大值，左数组为最大堆，右数组为最小堆，
当右数组规模等于左数组时，我们把数加入右数组，再弹出右数组的最小值，加入左数组，
否则，当右数组规模小于左数组时，我们将数加入左数组，再弹出左数组的最大值，加入右数组（因为要维护左数组始终大于右数组）
由于python只有大顶堆，所以我们通过构建-num的大顶堆，这样弹出的最大值就是最小值对应的负数。
```python
  class MedianFinder(object):

    def __init__(self):
        self.small = []
        self.large = []
        

    def addNum(self, num):
        """
        :type num: int
        :rtype: None
        """
        if len(self.small) == len(self.large):
            heappush(self.small, - heappushpop(self.large, num))
        else:
            heappush(self.large, - heappushpop(self.small, - num))
        

    def findMedian(self):
        """
        :rtype: float
        """
        if len(self.small) > len(self.large):
            return - self.small[0]
        return (- self.small[0] + self.large[0]) / 2.0
```


## 每日碎碎念
* 好好调整。
