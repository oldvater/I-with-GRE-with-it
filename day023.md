# 2026.1.13
布豪，发呆发了一天，明明无聊得要命，但就是不想学习，场地buff开起来了这一块。

## 力扣每日一题:
  994.腐烂的橘子
  [https://leetcode.cn/problems/rotting-oranges?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 对不起，这才是水漫金山，典型的bfs思维，每次进入新的一轮时ans就加1，知道全腐烂或者存储下一层为空时停止
  ，对于每一个元素，都去上下左右扩散，存储可以采用列表或者两个数组，一个继承上一层，一个单开一个本层来进行bfs遍历
  ，同时引入fresh来记录是否全部腐烂，最开始全局遍历一次，记录所有fresh数量和初始腐烂橘子所在位置，然后bfs遍历即可。
  ```python
  class Solution(object):
    def orangesRotting(self, grid):
        """
        :type grid: List[List[int]]
        :rtype: int
        """
        ans = 0
        fresh = 0
        q = deque()
        m, n = len(grid), len(grid[0])
        for i, row in enumerate(grid):
            for j, x in enumerate(row):
                if x == 1:
                    fresh += 1
                elif x == 2:
                    q.append((i, j))
        
        while q and fresh:
            ans += 1

            for i in range(len(q)):
                a = q.popleft()
                x, y = a[0], a[1]
                for i, j in (x - 1, y), (x + 1, y), (x, y - 1), (x, y + 1):
                    if 0 <= i < m and 0 <= j <n and grid[i][j] == 1:
                        grid[i][j] = 2
                        fresh -= 1
                        q.append((i, j))

        return -1 if fresh else ans


        

## 每日碎碎念
* 布豪，今天刷了一天抖音，信息获取几乎为0，该学了
* 今天又复盘了一下清华，有一说一，清华真的符合我对大学的全部幻想：不是更大的操场，更大的教学楼，更大的宿舍，清华给我一种生活在小镇的感觉一样。一见清华误终身啊，还好我不是高中时候去的，不然我估计就认不清定位妄图挑战华子了，但是说不定能让我更上进点？
