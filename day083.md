# 2026.3.17
沃柑，布豪，这老师直接签到，抓我timing。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 1/8 | 7 | 开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 3)
- **目标单元**：[数据结构（栈，堆）]
- **周期剩余**：6 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2441.与对应负数同时存在的最大正整数
  [https://leetcode.cn/problems/largest-positive-integer-that-exists-with-its-negative]

## 思路：
- 枚举右边，维护左边，这题本质就是两数之和为0，思路是维护一个哈希表，，记录出现的所有数，对于一个数，如果-x在前面出现过，在哈希表里，
  则更新答案为x，-x，ans里的最大值（毕竟可能先出现正的后出现负的，不过最大的肯定不是正的就是ans）。
```python
  class Solution(object):
    def findMaxK(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        d = {}
        ans = -1
        for x in nums:
            if -x in d:
                ans = max(x, -x, ans)
            else:
                d[x] = 1
        return ans
        
```
- 2016.增量元素之间的最大差值
  [https://leetcode.cn/problems/maximum-difference-between-increasing-elements]

## 思路：
- 依旧枚举右，维护左，这题要维护的是前面出现的最小值，对于一个往后遍历的数组里的值，它的最大差值一定是当前值减去前面遇到过的最小值的差
  （因为题目里明确说明了j>i，因此不用考虑后面会出现最大值在最小值前面的情况），而对于一个数，如果大于minval的话，就让ans与其差值
  作比较，更新较大的数，如果小于等于minval的话，minval就更新为该值。
```python
  class Solution(object):
    def maximumDifference(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        minv = nums[0]
        ans = -1
        for x in nums:
            if x > minv:
                ans = max(ans, x - minv)
            else:
                minv = x
        return ans
```
- 624.数组列表中的最大距离
  [https://leetcode.cn/problems/maximum-distance-in-arrays]

## 思路：
- 依旧枚举右维护左这一块（bushi），看到这个题目，很容易想到上个题目：维护最小值，用后续值减去最小值从而不断更新最大值。
  但是，这题有两点特殊：1.给的是多个数组，两个数必须从不同数组里选取；2.给的是排序数组。
  那不更简单了？直接选择一个数组，不断维护它之前的最小值，然后用这个数组的最大值，也就是最后一个元素去减去最小值，
  更新ans，然后再将最小值和当前数组最小值中更小的更新为新最小值不就行了吗。
  在遇到某个神秘测试案例前，我也是这么想的，测试案例如下：
  ```
  [[1,2,5],[0,5]]
  ```
  如果选择往右枚举的话，这个案例只能得到右边的最大值减左边的最小值得到4，但显然，左边的最大值减右边的最小值更大一些。
  难道要把时间乘2，专门加一个反转逻辑吗？显然这不是代码题要考虑的事（如果是项目的话我真就这么搞了（bushi）），
  代码题要做的就是一次尽可能完成所有任务。那么该怎么办呢？注意到，***左边的最大值-右边的最小值***和
  ***右边的最大值-左边的最小值***在结构上是非常相似的，那么在代码上，只需要再维护一个前面的最大值不就好了吗，然后计算答案的时候，
  就维护max（ans，|s[-1]-min1|, |m1 - s[0]|）（为了防止某些神秘测试案例，搞出来一个前面的最小值非常非常大，
  而后面的最大值特别特别小的情况，因此加一个绝对值保险）
```python
  class Solution(object):
    def maxDistance(self, arrays):
        """
        :type arrays: List[List[int]]
        :rtype: int
        """
        
        ans = -1
        min1 = arrays[0][0]
        m1 = arrays[0][-1]
        for i in range(1, len(arrays)):
            s = arrays[i]
            ans = max(ans, abs(s[-1] - min1), abs(m1 - s[0]))
            min1 = min(min1, s[0])
            m1 = max(m1, s[-1])
        return ans
```

## 考研每日总结
- 408：ds：树：前三小节。
- 数学：第五讲一半。

## 每日碎碎念
* 这老师怎么不讲武德，偷偷搞签到，麻了。
* 神了，学个数据结构大概要学20天，感觉有点学不完了，麻了。
* 没事，如果7月之前一轮没过完就转控制了，还来得及这一块，及时跳车这一块。
* 淦，想考软微了，照着软微学吧，今天看了一小时的软微经验贴，给我看热血沸腾了。
* 不过正确率还算不赖，选择题90%的正确率，还行吧，起码不是学得又慢正确率又低，那才是真完蛋了。
