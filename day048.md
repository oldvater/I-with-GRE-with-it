# 2026.2.8
洋湖湿地公园挺大，比长春那个什么湿地公园好一点感觉。

## 力扣每日一题:
55.跳跃游戏
  [https://leetcode.cn/problems/jump-game?envType=study-plan-v2&envId=top-100-liked]

## 思路：
只需要维护一个目前能够到达的最远位置，然后顺序遍历，当顺序遍历的下标i大于最远距离时，意味着无法到达i，返回False，
如果mx大于等于n-1，说明能够到达尾部，返回True。
```python
  class Solution(object):
    def canJump(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        n = len(nums)
        i = 0
        mx = 0
        for i, x in enumerate(nums):
            if i > mx:
                return False
            mx = max(mx, i + x)
            if mx >= n - 1:
                return True

```


## 每日碎碎念
* 洋湖湿地公园好玩，就是太远了，有没有地铁站附近的公园啊。
* 累死了今天，不过起码是调整了一下状态
* 最多的第四章快看完了，估计年前可以肝到第六章，然后年后赶三章即可，美滋滋。
