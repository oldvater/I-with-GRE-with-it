# 2026.3.3
元宵快乐！hot100和稿子同时结束，爽！

## 力扣每日一题:
287.寻找重复数
  [https://leetcode.cn/problems/find-the-duplicate-number?envType=study-plan-v2&envId=top-100-liked]

## 思路：
- nb，算法思想还是太变态了，数组题可以转化成链表用环形链表来做。
  思路就是：由于只有一个重复的数，因此1到n中的任意数出现的次数一定大于等于1，且只有一个为2.
  我们就可以构建p.next = nums[p],从下标p指向对应地点存储的数，由于只有一个频数为2，因此只会有一个节点会被指向两次，
  即入度为2，且所有节点的出度都为1（当然，下标0的位置入度为0，因为数组中不存在0），因此从0开始，必然可以遍历出整个链表
  （从其他节点开始的话可能会导致环型结构导致节点循环，因为其他节点有入度，可能传回来，而0不需要考虑这种情况）。
  就变成了寻找入度为2的节点，即环形链表的入环点，快慢指针即可。当slow和fast相等时，此时离入环点还差a距离，
  这个a刚好为链表开头，也就是0的位置到入环点的距离，所以当slow和head相等时，即为入环点，也就是重复的数。
```python
  class Solution(object):
    def findDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        slow = fast = 0
        while True:
            slow = nums[slow]
            fast = nums[nums[fast]]
            if slow == fast:
                break
        
        head = 0
        while head != slow:
            head = nums[head]
            slow = nums[slow]
        
        return slow
```


## 每日碎碎念
* 完美完结。
* 有一说一，要考研了还是有点紧张的，今年11408 400分遍地走，390不如狗，ε=(´ο｀*)))唉，努力一年看看吧。
* 第一阶段hot100刷完了，接下来进入题单阶段。
  讲解规则：由于灵神的题单题太多了，一天一道的话可能考完研了我还在链表，因此我决定跳跃刷题，规则如下：
  1.按照hot100给出的权重，分配天数给不同的专题，比如链表有22题，数组有5题，所以链表给22天，数组给5天；
  2.设立熔断机制：防止1中链表的22题毁了手感，规定，一个专题最多连刷7天，7天内刷完权重就换下一个专题，
  没刷完，将刷题情况压入队列，等待所有专题都按照上述规则刷一遍时，按照先入先出的原则开始刷剩下的。
  比如：权重是链表：22,哈希表：5,图：7,动态规划：13，那么刷题顺序为：
  链表7天（剩余15天）->哈希表5天->图7天->动态规划7天（剩余6天）->链表7天（剩余8天）->动态规划6天->链表8天，完成。
  列个表记录每天数据先。
# 算法刷题进度表 (螺旋上升法)

## 核心规则
1. **总预算** = 该单元在 Hot100 中的题目数量。
2. **熔断机制** = 单个单元连续刷题不超过 **7天**。
3. **每日任务** = 1道精写 (Hot100/经典) + 1道速看 (新题)。

## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 3 | 二分查找 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


## 当前周期 (Week 1)
- **目标单元**：[数组/哈希]
- **周期剩余**：7 天 (熔断倒计时)
- **今日必做**：Hot 100 第一题
- **今日选看**：新题单对应分类第 1-2 题

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
