# 2026.1.14
坏了，情况不减反增，我被抖音b站所伤已久，竟落得如此下场，传我命令，即刻，戒快手！得学会了，我发现我就是越无聊就越无聊。

## 力扣每日一题:
  207.课程表
  [https://leetcode.cn/problems/course-schedule?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- bfs:首先使用indegree和adj分别存储一个课程的前置课程数和修完了以后可以解锁的课程，然后创建一个列表，将所有前置课程数为0的课程放入列表
  中，记为可以直接修读的课程，然后排出课程，视为修读过了该课程，这时候遍历所有以这门课为前置课程的课，对应的indegree减一，若indegree为
  0，则说明可以修读，加入列表，同时记录已修读的课程数k，若k < 总课程数num，则返回false，否则返回true。
  ```python
  class Solution(object):
    def canFinish(self, numCourses, prerequisites):
        """
        :type numCourses: int
        :type prerequisites: List[List[int]]
        :rtype: bool
        """
        indegree = [0] * numCourses
        adj = [[] for _ in range(numCourses)]
        for cur, pre in prerequisites:
            indegree[cur] += 1
            adj[pre].append(cur)

        q = deque()
        for i in range(numCourses):
            if indegree[i] == 0:
                q.append(i)

        k = 0
        while q:
            pre = q.popleft()
            k += 1

            for cur in adj[pre]:
                indegree[cur] -= 1
                if indegree[cur] == 0:
                    q.append(cur)
        if k < numCourses:
            return False
        else:
            return True
- dfs:用三色染色法，只需找到一个正在遍历过程中的环即可，首先建立一个图集，和颜色集colors，对于未经过的点，对应的colors为0
  ，对于正在递归中但还未返回的colors为1，对于已经完全访问的colors为2，要找到冲突课程，无非就是在通过课程搜索的时候出现了环，
  所以dfs函数的返回条件有：1.现在就是环；2.下一个节点开始是环（因为要考虑下一个节点可能已经被其他课程遍历时访问过导致的错误，
  得加一个未访问的约束）。若上述条件均不满足，则把该点标记为已经过，返回false。在遍历完所有未经过的点后，如果有一个点dfs返回
  有环，则返回false，否则就是true。
  ```python
  class Solution(object):
    def canFinish(self, numCourses, prerequisites):
        """
        :type numCourses: int
        :type prerequisites: List[List[int]]
        :rtype: bool
        """
        g = [[] for _ in range(numCourses)]
        for cur, pre in prerequisites:
            g[pre].append(cur)

        colors = [0] * numCourses
        def dfs(pre):
            colors[pre] = 1
            for cur in g[pre]:
                if colors[cur] == 1:
                    return True
                elif colors[cur] == 0 and dfs(cur):
                    return True
                
            colors[pre] = 2
            return False
        for i, c in enumerate(colors):
            if c == 0 and dfs(i):
                return False
        return True
        

        

## 每日碎碎念
* 慢慢来吧，起码今天看了点演讲励志小视频
* 淦，家里有毒，学不进
