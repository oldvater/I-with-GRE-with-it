# 2026.3.16
感觉压力好大啊。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 2)
- **目标单元**：[二分查找]
- **周期剩余**：0 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 1870.准时到达的列车最小时速
  [https://leetcode.cn/problems/minimum-speed-to-arrive-on-time]

## 思路：
- 这类题型，难点就两个：1.找区间；2.写check函数。本题的check函数的实现逻辑是对于给定的时间mid，
  dist数组中除了最后一项都要除以mid然后向上取整（代表必须从整点出发，早到也得等到整点），然后累加，再加上最后一个数/mid（不需要等发车了），
  这时候如果时间小于hour的话，说明能到达，返回True。然后这题又是一个越高越好的，即大于满足的一定满足，因此找最小，主函数二分即可。
- 这题由于有浮点数，运算速率可能会慢一点，不过由于最多存两位数字，可以考虑check函数中值都乘100，这样就没有浮点运算了。
- 这题的left是0（速度为0，肯定不行），right为10e7+1（题目给出的，依旧近光速飞船这一块）。
```python
  class Solution(object):
    def minSpeedOnTime(self, dist, hour):
        """
        :type dist: List[int]
        :type hour: float
        :rtype: int
        """
        n = len(dist)
        h100 = round(hour*100)
        left, right = 0, 10e7 + 1
        s = 0
        if n - 1 >= hour:
            return -1
        def check(mid):
            s = 0
            for i, x in enumerate(dist):
                if i < n - 1:
                    t = x // mid if x % mid == 0 else x // mid + 1
                else:
                    t = x / mid
                s += t
            if s*100 <= h100:
                return True
            return False
            
        while left + 1 < right:
            mid = (left + right)//2
            if check(mid):
                right = mid
            else:
                left = mid
        return int(right)
            
```
- 275.H指数II
  [https://leetcode.cn/problems/h-index-ii]

## 思路：
- 这题的check函数的实现原理是：对于数mid，由于citations数组是有序数组，因此直接找倒数第mid个数，如果大于等于mid，那就说明满足，
  返回True。这题是越小越好（h指数等于0肯定能满足吧，但h指数破100就难如登天了），因此是找最大值问题。转变一下更新逻辑，变成满足时left=mid，
  不满足是right=mid，这样最后返回的mid就是满足条件的最大值。
- 这题的left是-1（不排除为0的可能，比如我（bushi）），
  right为n+1（毕竟真的有可能有神人发了多少篇，每篇的引用量都大于总数，也有可能只有1篇）。
```python
  class Solution(object):
    def hIndex(self, citations):
        """
        :type citations: List[int]
        :rtype: int
        """
        n = len(citations)
        def check(mid):
            if citations[-mid] >= mid:
                return True
            return False
        left, right = -1, n+1
        while left + 1 < right:
            mid = (left + right)//2
            if check(mid):
                left = mid
            else:
                right = mid
        return left
        
```
- 2226.每个小孩最多能分到多少糖果
  [https://leetcode.cn/problems/maximum-candies-allocated-to-k-children]

## 思路：
- check函数实现：对于给定的数mid，将candies数组里的所有数整除mid以后求和，最后得到的数就是按照每个孩子mid的量能分给多少个孩子
  （因为是分堆，对于小于mid的值不能分给孩子，大于mid的值也得根据mid来分成几堆，向下取整），如果大于等于k，就返回True。
  这题依旧是值越小越好，即小于满足的肯定满足（每个人分0个糖果，剩下我全拿走不满足条件吗），因此是找最大值问题，二分即可。
- 这题的left是0（上面说了，每个孩子都没有糖果也满足题意），right是sum(candies)//k+1（每个人分的量超过平均数了还想平分就不可能了）。
- 还可以加入一些优化，比如开局判断总和，如果小于k的话，说明每个孩子最多分0个糖果，直接返回0即可。
```python
  class Solution(object):
    def maximumCandies(self, candies, k):
        """
        :type candies: List[int]
        :type k: int
        :rtype: int
        """
        n = len(candies)
        total = sum(candies)
        if total < k:
            return 0
        def check(mid):
            s = 0
            for i, x in enumerate(candies):
                s += x // mid
            if s >= k:
                return True
            return False
        left, right = 0, total // k + 1
        while left + 1 < right:
            mid = (left + right) // 2
            if check(mid):
                left = mid
            else:
                right = mid
        return left
```

## 考研每日总结
- 408：无。
- 数学：第四讲+对应1000题。

## 每日碎碎念
* 沟槽的早八，直接吞我半天复习时间。
* wc，xky请我喝奶茶，爽！
* 有点小焦虑了，发现数学加速的话，专业课就有点难跟上，其实主要还是学期内课程太多了，然后那群老师又不放开，
  还得每节课去听，ε=(´ο｀*)))唉，麻了。
* 骗你的，寄生那些去听课的不就行了？
* 1000题写到后面有点昏头脑涨了，记得不要搞混了n阶求导公式和泰勒展开的第n项（往往n阶求导公式往泰勒那个通项里套进去就是泰勒展开的第n项），
  不能全记公式，但是公式得记。
* 加油吧，会学完的。
