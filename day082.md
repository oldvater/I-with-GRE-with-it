# 2026.3.15
休息一天！爽！火焰烧烤自助，不赖。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 5/6 | 1 | 开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 2)
- **目标单元**：[二分查找]
- **周期剩余**：1 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2476.二叉搜索树最近节点查询
  [https://leetcode.cn/problems/closest-nodes-queries-in-a-binary-search-tree]

## 思路：
- wc，这题26考研408的算法题考了个类似的，不过是这个的简化版，那题就直接二叉搜索树遍历就行，这题得转化成有序数组才行。
  首先对于queries里的多项，对于每项都进行二分显然不行，时间复杂度到了O（nlogm），因此得前序遍历二叉搜索树（二叉搜索树必有序），
  得到有序数组res，那么小于等于的最大值就是严格大于的前面一项，而大于等于的最小值就是严格小于的后面一项。
  但是由于bisect函数的实现，bisect_left本身是严格小于的的后一项，bisect_right是严格大于的当项（因为是把数插入进去应该排的位置）。
  因此，min的值是res[i-1],max的值就行res[j],当然，还得判断i和j，对应i=0和j=n的情况，这时候说明要找的值不存在，赋值-1即可。
  最后把min和max合成列表加入答案ans，返回ans即可。
- 说来惭愧，这最后的逻辑昨天把我猪脑过载了，还是靠ai才捋清。
```python
  # Definition for a binary tree node.
# class TreeNode(object):
#     def __init__(self, val=0, left=None, right=None):
#         self.val = val
#         self.left = left
#         self.right = right
class Solution(object):
    def closestNodes(self, root, queries):
        """
        :type root: Optional[TreeNode]
        :type queries: List[int]
        :rtype: List[List[int]]
        """
        res = []
        def inorder(node):
            if node is None:
                return
            inorder(node.left)
            res.append(node.val)
            inorder(node.right)
        
        ans = []
        inorder(root)
        n = len(res)
        for x in queries:
            # 寻找 min_val (小于等于 x 的最大值)
            # bisect_right 返回插入点，该点左侧的所有值都 <= x
            # 所以我们需要查看 index - 1
            i = bisect.bisect_right(res, x)
            min_val = res[i-1] if i > 0 else -1
            
            # 寻找 max_val (大于等于 x 的最小值)
            # bisect_left 返回插入点，该点出的值 >= x (如果存在的话)
            j = bisect.bisect_left(res, x)
            max_val = res[j] if j < n else -1
            
            ans.append([min_val, max_val])
            
        return ans
            
```
- 1283.使结果不超过阈值的最小除数
  [https://leetcode.cn/problems/find-the-smallest-divisor-given-a-threshold]

## 思路：
- 首先写一个check函数，功能是判断给的x能不能满足条件，实现逻辑是遍历nums里的数num，将所有的num//x后相加，判断是否大于threshold，
  也就是题干中给出的判断的条件。然后在主函数中，由于这题的答案是递增的，对于一个数，如果它满足条件的话，那么大于它的数都满足条件，
  这时候就可以通过二分查找来实现查找满足条件的最小值，对于一个mid，用check来判断一下它是否满足条件，满足的话，right=mid，
  不满足的话就left=mid（开区间），这样子能保证最后的right一定是满足条件的最小值（因为循环退出条件是left+1=right，
  而left必然是不满足条件的）最后返回right即可。
- 这类题型的难点主要是找left和right，一般不想优化的话left肯定是0，right肯定是越大越好（已知最好是满足条件的最小值）。
```python
  class Solution(object):
    def check(self, nums, threshold, x):
        s = 0
        for x1 in nums:
            tmp = x1 // x if x1 % x == 0 else x1 // x + 1
            s += tmp
        if s > threshold:
            return False
        return True
    def smallestDivisor(self, nums, threshold):
        """
        :type nums: List[int]
        :type threshold: int
        :rtype: int
        """
        left, right = 0, max(nums)
        while left + 1 < right:
            mid = (left + right)//2
            if self.check(nums, threshold, mid):
                right = mid
            else:
                left = mid
        
        return right
        
```
- 2187.完成旅途的最少时间
  [https://leetcode.cn/problems/find-the-smallest-divisor-given-a-threshold]

## 思路：
- 这题的check条件就是给定时间t整除time中旅途所需时间x得到t时刻完成的旅途数，把它们累加求和，大于等于totalTrips就说明满足条件了。
  然后就是经典的二分查找（同样的因为对于某个时间t，如果满足条件的话，那后面的一定满足，不满足条件的话，前面的一定不满足），
  找到返回right即可。
- 这题的left是min(time)-1（这是总旅途数=0最大的left，同时也是所有不满足情况里的最大值，可能出现total=1的情况）,
  right是min(time)*totalTrips（这能保证就算是最小的time，也一定能慢慢跑完）。
```python
  class Solution(object):

    def minimumTime(self, time, totalTrips):
        """
        :type time: List[int]
        :type totalTrips: int
        :rtype: int
        """
        count = []
        left, right = min(time) - 1, min(time) * totalTrips
        while left + 1 < right:
            s = 0
            mid = (left + right)//2
            for i, x in enumerate(time):
                s += mid // x
            if s >= totalTrips:
                right = mid
            else:
                left = mid
        return right

```

## 考研每周总结
- 408：数据结构前四讲：基本思想、线性表、栈和队列、串。
- 数学：前三讲+对应1000题。

## 每日碎碎念
* 学习得加速了，下周接着提速。
* 帮别人搞了一个压力传感器的读取，AI真好用啊，虽然调试还得自己调。
* 这周结束，下周继续。
