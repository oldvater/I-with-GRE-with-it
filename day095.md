# 2026.3.30
数学第九讲课看完了，明天写1000题加上结束数据结构，这月任务完美完成，吗？



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 7/8 | 1 | 🔄 挂起 | |
| 5 | 链表 | 14  | 5/14 | 9 | 开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 4)
- **目标单元**：[链表]
- **周期剩余**：2 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2487.从链表中移除节点
  [https://leetcode.cn/problems/remove-nodes-from-linked-list]

## 思路：
- 对于一个节点，如果它右侧有更大数值，那我们就可以从尾端开始往前遍历，问题就变成了：删除所有值小于前面值最大节点的节点。
  就变成了维护前面最大值，然后判断当前节点的值，如果小于，就删除，否则就更新当前节点值为最大值。
  最后再将处理后的反向链表再反转就行。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def reverse(self, head):
        pre = None
        cur = head
        while cur:
            nxt = cur.next
            cur.next = pre
            pre = cur
            cur = nxt
        return pre
    def removeNodes(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        h1 = h = self.reverse(head)
        while h.next is not None:
            if h.next.val < h.val:
                h.next = h.next.next
            else:
                h = h.next
        return self.reverse(h1)
```
- 2807.在链表中插入最大公约数
  [https://leetcode.cn/problems/insert-greatest-common-divisors-in-linked-list]

## 思路：
- 写一个计算最大公约数的函数，然后主函数中，对于一个节点，如果它有下一个节点，说明这两节点之间可以插入最大公约数，计算出最大公约数，
  然后插入节点，注意h跳转的时候要连跳两次（因为h的next接了新节点）。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def max_num(self, num1, num2):
        if num2 == 0:
            return num1
        return self.max_num(num2, num1 % num2)
    def insertGreatestCommonDivisors(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        if head is None or head.next is None:
            return head
        h = head
        while h.next:
            num = self.max_num(h.val, h.next.val)
            new_node = ListNode()
            new_node.val = num
            new_node.next = h.next
            h.next = new_node
            h = new_node.next
        return head
```

## 考研每日总结
- 408：8.3完成。
- 数学：第九讲课看完。

## 每日碎碎念
* 数学完成，但是408没有完成，明天上午写1000题之后，下午全力进攻数据结构，务必完成。
* rag项目添加了langfuse监控和多智能体协同，现在已经有两个llm在干活了，一个负责审查，一个负责生成。
