# 2026.1.28
计组看完湖科大的了，估计是还差个IO，不急，等一轮再说，可以开操作系统和计网了。

## 力扣每日一题:
  153.寻找旋转排序数组中的最小值
  [https://leetcode.cn/problems/find-minimum-in-rotated-sorted-array?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 这就是我昨天说的寻找旋转点了
  思路：将nums[mid]和nums[-1]对比，大于则更新left，否则更新right。返回right的值（因为nums[right]一定在最小值线上）
  （因为要制造一个返回点，如果是闭区间的话，left=right=旋转点时无法返回，当然，left=mid+1也可以认为制造返回点）。
  ```python
  class Solution(object):
    def findMin(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        left, right = 0, n - 1
        while left < right:
            mid = (left + right) // 2
            if nums[mid] < nums[-1]:
                right = mid
            else:
                left = mid + 1
        return nums[right]


## 每日碎碎念
* 终于预习完了，现在起码对计组部分有所了解，大微机原理这一块。
