# 2026.2.25
孩子们，我做到了，一天完成一章。

## 力扣每日一题:
1143.最长公共子序列
  [https://leetcode.cn/problems/longest-common-subsequence?envType=study-plan-v2&envId=top-100-liked]

## 思路：
有一说一，目前发现了dp主要就是选或不选或者相加原理，这题就是选或不选，有两个字符串t1和t2与对应的下标i，j，
如果i和j对应的相等，那么就都选上（因为是从后面往前面选，两个字符串的公共部分几乎一定在公共最长的子序列中，
反证：如果存在一个公共部分同时选择会影响最后结果，那么改变的长度一定大于1，因为等于1的话不改变最终答案，
比如“abcd”和“adbc”，如果选择了“d”的话结果就会改变，但是，这种情况下，往往在“d”后面的“bc”部分已经得到答案了，
因此，不会改变结果，假设不成立），返回dfs（i-1，j-1）+1，如果不同，那就从i-1或者j-1中二选一，
即max（dfs（i-1，j）， dfs（i，j-1）），最后返回答案即可。
```python
  class Solution(object):
    def longestCommonSubsequence(self, text1, text2):
        """
        :type text1: str
        :type text2: str
        :rtype: int
        """
        m, n = len(text1), len(text2)
        cache = [[-1] * n for _ in range(m)]
        def dfs(i, j):
            if i < 0 or j < 0:
                return 0
            if cache[i][j] != -1:
                return cache[i][j]
            if text1[i] == text2[j]:
                res = dfs(i-1, j-1) + 1
            else:
                res = max(dfs(i-1, j), dfs(i, j-1))
            cache[i][j] = res
            return res
        
        return dfs(m-1, n-1)
        
```

## 每日碎碎念
* 一天看完一章，还剩三章，甚至只看页数的话只有今天看的2/3，难道我真的是赋能哥？
* 晚上打了会小乒乓，果然这玩意练了和没练的真的是天差地别。
