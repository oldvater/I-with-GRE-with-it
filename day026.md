# 2026.1.16
今天啃了个SE+Res框架的神经网络。

## 力扣每日一题:
  46.全排列
  [https://leetcode.cn/problems/permutations?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 普通回溯（恢复现场）：创建一个tmp数组，记录当前已排列的结果，如果还有未排列的数，则选一个加入tmp中，
  当未排列的数组为空时，说明tmp已经把nums的数全部存入，把tmp的副本存入ans，然后返回。
  当从下一层返回时，由于此刻的tmp已经存了当前数字排列的结果，因此要从tmp中删去之前存入的数，接着进行下一次存储（如果还有的话）
  当未存储的数全部存了一遍以后，函数结束，返回上一层。最后可以得到全排列。
  ```python
  class Solution(object):
    def permute(self, nums):
        """
        :type nums: List[int]
        :rtype: List[List[int]]
        """
        ans = []
        tmp = []
        def p1(nums):
            if not nums:
                ans.append(tmp[:])
                return
            for i, x in enumerate(nums):
                tmp.append(x)
                p1(nums[:i] + nums[i+1:])
                tmp.remove(x)
        
        p1(nums)
        return ans
- 记录足迹:和上面类似，只需要去管未排列的数就行，不过这个从对数组切片变成创建一个on_path列表，专门记录是否排列，
  只需要在返回时恢复现场，把on_path从True改为False即可。
  ```python
  class Solution(object):
    def permute(self, nums):
        """
        :type nums: List[int]
        :rtype: List[List[int]]
        """
        n = len(nums)
        ans = []
        path = [0] * n
        on_path = [False] * n
        def dfs(i):
            if i == n:
                ans.append(path[:])
                return
            for j in range(n):
                if not on_path[j]:
                    path[i] = nums[j]
                    on_path[j] = True
                    dfs(i + 1)
                    on_path[j] = False
        
        dfs(0)
        return ans

## 每日碎碎念
* 今天啃了个SE+Res框架的神经网络，我草，好繁杂啊，难也说不上，就感觉一半代码在处理数据，一半代码在
  构造模型，模型感觉就是搭梯子，用一堆基础的层，像Linear，Relu这些积木一点一点搭起来整个模型，有点好玩？
* 我感受到了动力！
* 德克士手枪腿味道还是不错，但就是肉太少了，可惜可惜
