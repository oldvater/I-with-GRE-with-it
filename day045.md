# 2026.2.5
依旧早睡早起，依旧慢慢学习。

## 力扣每日一题:
347.前K个高频元素
  [https://leetcode.cn/problems/top-k-frequent-elements?envType=study-plan-v2&envId=top-100-liked]

## 思路：
通过哈希字典记录nums中元素出现的频率，为cnt，同时记录最大出现频率max_cnt，建立桶bucket，将cnt中的键存储在对应下标中
，然后逆序读取桶，直到选到第k个元素。
```python
  class Solution(object):
    def topKFrequent(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: List[int]
        """
        ans = []
        cnt = Counter(nums)
        max_cnt = max(cnt.values())

        bucket = [[] for _ in range(max_cnt + 1)]
        for x, c in cnt.items():
            bucket[c].append(x)

        while len(ans) < k:
            ans += bucket[-1]
            bucket.pop()
        

        return ans
```


## 每日碎碎念
* 无事发生这一块。
