# 2026.1.17
今天爽玩这一块。

## 力扣每日一题:
  78.子集
  [https://leetcode.cn/problems/subsets?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 选or不选：函数内部，对于已有的tmp，有两个选择，继续or不继续，如果选择不继续往下的话，直接往ans里添加tmp就行，
  而继续的话就需要往tmp里加入该元素后面的元素（如果不限制后面的元素的话，会出现顺序不同的情况如[1,2]和[2,1]），
  接着进行判断，同时记得返回的时候要恢复现场。
  ```python
  class Solution(object):
    def subsets(self, nums):
        """
        :type nums: List[int]
        :rtype: List[List[int]]
        """
        ans = []
        tmp = []
        n = len(nums)
        def dfs(i):
            ans.append(list(tmp))
            for j in range(i, n):
                tmp.append(nums[j])
                dfs(j + 1)
                tmp.pop()
        
        dfs(0)
        return ans
- 数学归纳法:已知对于一个新加入的元素，子集为当前子集加入元素后的子集和原子集的和，因次对于每个元素，
  都有一个新子集，然后新子集加原子集即可。
  ```python
  class Solution(object):
    def subsets(self, nums):
        """
        :type nums: List[int]
        :rtype: List[List[int]]
        """
        ans = [[]]
        for i in nums:
            tmp = [cur + [i] for cur in ans]
            ans += tmp
        return ans

## 每日碎碎念
* 小知识：ipynb的工作区默认在当前文件夹，而py文件的工作区则不一定。因此，SummaryWriter记录日志时，
  ipynb在当前文件夹下，而py文件则在工作区文件夹下，不要找错了
* 对于两个人报数，先到指定数字者赢的问题。假设两者报数之和可以构成一个定值k，则当指定数为k的整数倍时，后者必赢
  ；否则前者必赢，前者只需要报数使剩余数为k的整数倍，那么就变成了k的整数倍时的后者。
