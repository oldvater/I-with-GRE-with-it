# 2026.3.22
电竞酒店这一块，玩美了这一块，由于电竞酒店不能登github昨天没更新没办法。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 5/8 | 3 | 开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 3)
- **目标单元**：[数据结构（栈，堆）]
- **周期剩余**：2 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2364.统计坏数对的数目
  [https://leetcode.cn/problems/count-number-of-bad-pairs]

## 思路：
- 有坏数对，那么就有好数对，注意到坏数对的定义：j-i!=nums[j]-nums[i],进行变换后可以变成nums[j]-j!=nums[i]-i(j>i),这被称为坏数对，
  那么给出好数对的定义：nums[j]-j = nums[i]-i(j>i)，可以看到坏数对的个数加上好数对的个数就等于总数对个数即n（n-1）/2，
  因此只要求好数对的个数即可，那么就是维护nums[j]-j的个数的字典，如果在字典里存在相同的数，那就是好数对，tmp加上对应的个数。
  最后用总数减去tmp即可。
```python
  class Solution(object):
    def countBadPairs(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        d = {}
        tmp = 0
        n = len(nums)
        for i, x in enumerate(nums):
            num = x - i
            if num in d:
                tmp += d[num]
                d[num] += 1
            else:
                d[num] = 1
        return n*(n-1)//2 - tmp
        
```
### 全新副本：周赛！###
- 101018.构造奇偶一致的数组I
  [https://leetcode.cn/problems/construct-uniform-parity-array-i]

## 思路：
- 神秘脑筋急转弯题，恒正确这一块，首先，以单个元素为例，那么分数组一定满足条件，对于长度大于一的数组，全奇全偶的情况就不说了，
  只要存在一个奇数，那么所有奇数都是奇数，所有偶数减去奇数也是奇数，因此一定能够构造出全奇或全偶的数组。
```python
  class Solution(object):
    def uniformArray(self, nums1):
        """
        :type nums1: List[int]
        :rtype: bool
        """
        return True
```
- 3876.构造奇偶一致的数组II
  [https://leetcode.cn/problems/construct-uniform-parity-array-ii]

## 思路：
- 比上一个题有难度，加了一个num[j]-num[i]必须大于0的限制，依旧没要求顺序。
- 那么我们可以注意到，对于nums1中最小的元素，它在新数组里的构造方法只能是原数带过去（因为它比其他数都小），
  因此nums2的奇偶取决于最小元素的奇偶，如果最小元素是奇数，那么一切好说，后面的奇数直接带进去，偶数减去奇数就行，
  但如果是偶数的话，如果后续存在奇数，那么最小的那个奇数无法变化成偶数（只能直接带过去或者减去小于它的偶数，都是奇数），
  因此这时只要遍历数组nums1，存在奇数直接返回False即可。
```python
  class Solution(object):
    def uniformArray(self, nums1):
        """
        :type nums1: List[int]
        :rtype: bool
        """
        minv = min(nums1)
        if minv % 2 == 0:
            for x in nums1:
                if x % 2 == 1:
                    return False
        return True
```

## 考研每日总结
- 408：图：前三小节。
- 数学：第六讲彻底结束，但仍需要不断复习。

## 每日碎碎念
* wc，电竞酒店，爽！
* 迷核强啊，埃姆雷也强。
* rag主要还是靠embeding的向量。
* 这周没有把图完成，可惜了，没事，不影响4月之前结束数据结构。
* 有一说一，周赛最大的难受点就是只能提交，不能调试，只能纯靠鬼脑去想。
