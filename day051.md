# 2026.2.11
我的天哪快手极速版大人。

## 力扣每日一题:
70.爬楼梯
  [https://leetcode.cn/problems/climbing-stairs?envType=study-plan-v2&envId=top-100-liked]

## 思路：
最经典的题目，最开始是在cs61a的递归里见到它，那个时候我还不知道动态规划，看傻了，后来明白了动态规划，本质就是大递归。
本质是数学归纳法，可以看到，n阶只能有n-1和n-2到达，因此climb(n)=climb(n-1)+climb(n-2)，写递归就行了
吗？当我写好了下面这个最简单的递归后，出现了超时，因为递归的层数太长了，每一层都要重复计算很多遍。
```python
  class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        if n == 1:
            return 1
        if n == 2:
            return 2
        return self.climbStairs(n - 1) + self.climbStairs(n - 2)
```
因此，可以尝试用哈希表记录每个数的climb值，这样当第二次及以后运行到climb（i）时，都可以直接从哈希表中获取，python可以直接用
@cache修饰器（虽然我没用成，但是，我的天哪python大人）
```python
  class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        cache = [-1] * (n+1)
        def dfs(i):
            if i == 1:
                return 1
            if i == 2:
                return 2
            if cache[i] != -1:
                return cache[i]
            res = dfs(i - 1) + dfs(i - 2)
            cache[i] = res
            return res
        return dfs(n)
```
由于这个的数学计算式和斐波那契数列的数学计算式差不多，就是初值不同，因此也可以采用计算斐波那契数列的递推算法。
```python
  class Solution(object):
    def climbStairs(self, n):
        """
        :type n: int
        :rtype: int
        """
        f0 = f1 = 1
        for _ in range(2, n + 1):
            new_f = f0 + f1
            f0, f1 = f1, new_f
        return f1
```


## 每日碎碎念
* 快手极速版神了，送太多了。
* 到达冷江。
* 不知道为什么有点心里堵，希望只是因为我睡少了。
* 悖论模拟全刷完了，拿了快30抽，我的天哪后备隐藏能源大人。
