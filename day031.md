# 2026.1.21
我草，好勾八冷。

## 力扣每日一题:
  79.单词搜索
  [https://leetcode.cn/problems/word-search?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 这种棋盘类问题在搜索中其实算简单的，只需要把握好退出的条件就可以完成，如本例，如果不在棋盘内（not (0 <= i < m and 0 <= j < n)）
  或者对不上（board[i][j] != word[l]），直接返回即可，然后修改当前内容。视为已访问，再四向递归搜索就行，记得再递归完成后
  把当前内容修改回来，让其他线路访问。
  ```python
  class Solution(object):
    def exist(self, board, word):
        """
        :type board: List[List[str]]
        :type word: str
        :rtype: bool
        """
        m, n = len(board), len(board[0])
        k = len(word)
        def find(i, j, l):

            if not (0 <= i < m and 0 <= j < n) or board[i][j] != word[l]:
                return False

            if l == k - 1:
                return True
            
            tmp, board[i][j] = board[i][j], '/'
 
            res = (find(i - 1, j, l + 1) or
                   find(i + 1, j, l + 1) or
                   find(i, j - 1, l + 1) or
                   find(i, j + 1, l + 1))

            board[i][j] = tmp
            return res

        for i in range(m):
            for j in range(n):
                if find(i, j, 0):
                    return True
        return False

- 优化方法：1.可行性剪枝：如果word里的某个字母数比board的对应字母数多的话，直接返回False即可。
           2.顺序剪枝：可以选择word首尾字母在board上对应出现次数最少的字母入手。因为在查找路径长度相同的前提下，出现次数少的开的线路更少
## 每日碎碎念
* 明天终末地开服，看看怎么个事。
