# 2026.2.24
布豪，今天真玩了一天，明天补吧，没办法了，休息两天了。

## 力扣每日一题:
5.最长回文子串
  [https://leetcode.cn/problems/longest-palindromic-substring?envType=study-plan-v2&envId=top-100-liked]

## 思路：
没看懂和动态规划有什么关系，有一说一。
就是遍历每个字符，然后以每个字符为中心去寻找各自的最长回文子串，同时记录。
判断是否回文，只需要已知L和R中间的部分是回文串，且L和R对应的字符相等，那么就是回文串，L-1，R+1，往外扩大。
对于奇数长度回文串，初始L=R=i，对于偶数长度回文串，初始L=i，R=i+1
```python
  class Solution(object):
    def longestPalindrome(self, s):
        """
        :type s: str
        :rtype: str
        """
        res_start = res_len = 0
        n = len(s)
        for i, c in enumerate(s):
            L = R = i
            while L >= 0 and R < n and s[L] == s[R]:
                if R - L + 1 > res_len:
                    res_len = R - L + 1
                    res_start = L
                L -= 1
                R += 1
            
            L = i
            R = i + 1
            while L >= 0 and R < n and s[L] == s[R]:
                if R - L + 1 > res_len:
                    res_len = R - L + 1
                    res_start = L
                L -= 1
                R += 1
        
        ans = ""
        for i in range(res_start, res_start + res_len):
            ans += s[i]
        return ans
        
        
```

## 每日碎碎念
* 明天开始赶进度了，只有十天就要出发去学校了，还有四章，应该能在去学校之前搞完。
* 看了无限城，有一说一，节奏有点磨叽，但是制作确实好。
