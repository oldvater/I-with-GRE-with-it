# 2026.3.9
还行吧，今天渐入状态了。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 5/6 | 1 | 开始 | |
| 3 | 二分查找 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[滑动窗口与双指针]
- **周期剩余**：1 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 1208.尽可能使字符串相等
  [https://leetcode.cn/problems/get-equal-substrings-within-budget]

## 思路：
- 先创建一个数组，i对应的值为|s[i]-t[i]|的ascii码值，得到开销矩阵，然后创建一个滑动窗口，在累积开销小于限制时扩大右端，
  当大于限制时左端右移，减少总开销，在扩大右端的时候维护长度最大值即可。
  这题的左端移动条件是累积开销大于限制开销。
```python
  class Solution(object):
    def equalSubstring(self, s, t, maxCost):
        """
        :type s: str
        :type t: str
        :type maxCost: int
        :rtype: int
        """
        n = len(s)
        st = [0]*n
        for i in range(n):
            st[i] = abs(ord(s[i]) - ord(t[i]))
        
        ans = 0
        left = 0
        sums = 0
        for i, x in enumerate(st):
            sums += x
            if sums > maxCost and left <= i:
                sums -= st[left]
                left += 1
                continue
            ans = max(ans, i - left + 1)
        
        return ans
            
```
- 904.水果成篮
  [https://leetcode.cn/problems/fruit-into-baskets]

## 思路：
- 由于题目明确指出了最多两种水果，因此可以直接用last和sec来分别记录最近的水果种类，第二近的水果种类，
  当一个水果不为前两个记录的值的时候，就意味着要换数字了，此时更新ans，维护其为ans和right-left的最大值（因为此时的下标right是开区间）
  ，同时可以记录此时的下标last_st，这样在下次更换数字的时候可以直接令left = last_st即可。
```python
  class Solution(object):
    def totalFruit(self, fruits):
        """
        :type fruits: List[int]
        :rtype: int
        """
        left = ans = 0
        last = fruits[0]
        sec = -1
        last_st = 0
        for right, x in enumerate(fruits):
            if x == last:
                continue
            if x != sec:
                ans = max(ans, right - left)
                left = last_st

            last, sec, last_st = x, last, right ## 当x==sec时，相当于last和sec互换了一下。
                                                    同时，只有last改变（互换，变化）时，last_st才会更新成right，
                                                    也就是说last_st-1和last_st对应的值一定不同，计算时也不需要考虑。
        return max(ans, len(fruits)  - left)
```
- 1695.删除子数组的最大得分
  [https://leetcode.cn/problems/maximum-erasure-value]

## 思路：
- 创建一个哈希表st（最好是集合，因为好删除）记录元素出现的情况，遍历数组，当x在st里时，删除窗口左端的元素直到x不在st里为止，
  同时得分减去左边的元素值，窗口左端左移。对于不在st的情况，就往st里添加x，然后得分加x，维护得分的最大值即可，总体来说属于很常见的滑动窗口题
  （但是我偏偏没做出来，因为实在不想用集合，习惯用字典，结果字典删除不好删，集合的remove操作是优先删除前面的数，字典则不好操作，
  还是得都用啊，要不怎么说有这么多数据结构呢）
```python
  class Solution(object):
    def maximumUniqueSubarray(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        ans = sum1 = left = 0
        st = set()
        for i, x in enumerate(nums):
            while x in st:
                st.remove(nums[left])
                sum1 -= nums[left]
                left += 1
            st.add(x)
            sum1 += x
            ans = max(ans, sum1)

        return ans

```
## 考研每日进度
- 408：数据结构第二章完成一轮，王道选择题
- 数学：超实数看一半。

## 每日碎碎念
* 数学复习的还是不尽人意，但是还好吧，没那么焦虑了，起码感觉能在暑假之前复习完，加上除了微积分下的内容其他基本都过过一遍了，不慌。
* 有一说一，408现在学习已经进入心流了，爽！
* 明早试一试早上七点起来健能不能做到吧，做到了那么时间就很灵活了。
