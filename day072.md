# 2026.3.6
到达学校！



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 2/6 | 4 | 开始 | |
| 3 | 二分查找 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[滑动窗口与双指针]
- **周期剩余**：4 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 3439.重新安排会议得到最多空余时间I
  [https://leetcode.cn/problems/reschedule-meetings-for-maximum-free-time-i]

## 思路：
- 对于会议的要求：相对顺序保持不变，且时间不能重叠。因此，移动一个会议的时候，只能考虑在前一个会议和后一个会议之间。
  对于k个会议，我们可以得到k+1个空隔（0和第一个会议，k个会议之间，第k个会议和eventTime），移动一个会议，就是移动会议的gap。
  且会议的移动只会影响相邻的gap大小（总gap时间一定，那么这个gap减小，另一个gap就会增加对应的数值），
  想要最大化连续gap，只需要将与其相邻的k-1个gap化为0，那么该gap就会变成最大，数值为这k个gap之和。
  因此，问题就变成了，求k个相邻的gap相加的最大值，一眼定长滑动窗口。
```python
  class Solution(object):
    def maxFreeTime(self, eventTime, k, startTime, endTime):
        """
        :type eventTime: int
        :type k: int
        :type startTime: List[int]
        :type endTime: List[int]
        :rtype: int
        """
        ans = tmp = 0
        n = len(startTime)
        gap = [0]*n
        gap[0] = startTime[0]
        for i in range(1, n):
            gap[i] = startTime[i] - endTime[i-1]
        gap.append(eventTime - endTime[-1])
        for i, x in enumerate(gap):
            tmp += x
            ans = max(ans, tmp)
            left = i - k
            if left < 0:
                continue
            
            

            tmp -= gap[left]
        
        return ans
```
- 3652.按策略买卖股票的最佳时机
  [https://leetcode.cn/problems/best-time-to-buy-and-sell-stock-using-strategy]

## 思路：
- 首先将所有值按prices*strategy的形式累加，然后开始滑动窗口，创建两个相邻的滑动窗口，第一个为mid~i的后k/2个元素，
  添加时需要将对应的tmp计算为strategy改为1时的情况，即“-1”时加两倍，“0”时加一倍，然后第二个窗口，为left~mid的前k/2个元素的窗口，
  这时前一个窗口的删去对应了此窗口的添加，因此合并起来，即从“1”到“0”的过程，直接减prices[mid]即可（注意mid为i-k/2,不需要+1，+1的话是后面窗口的开头，而不是前一个窗口的末尾）而该窗口的删去则是从“0”改回原来的strategy[left]，为“-1”时，减prices[left],为“1”时，加prices[left]。
```python
  class Solution(object):
    def maxProfit(self, prices, strategy, k):
        """
        :type prices: List[int]
        :type strategy: List[int]
        :type k: int
        :rtype: int
        """
        ans = 0
        tmp = 0
        for i, x in enumerate(prices):
            ans += x*strategy[i]
        tmp = ans
        for i, x in enumerate(strategy):
            mid = i - k/2
            left = i - k + 1
            
            if x == -1:
                tmp += 2*prices[i]
            elif x == 0:
                tmp += prices[i]

            if mid < 0:
                continue
            
            tmp -= prices[mid]
            
            if left < 0:
                continue
            
            ans = max(ans, tmp)

            if strategy[left] == -1:
                tmp -= prices[left]
            elif strategy[left] == 1:
                tmp += prices[left]
        
        return ans
```

## 每日碎碎念
* 到达学校！man！
* 果然还是不能都装在一个篮子里，还是得找实习，学考研两手准备，先这样看看吧。
* 万里闯关东铁锅炖好吃捏，给我吃爽了。
* 明天去健身房续卡吗？有点犹豫，看哈吉鲍明天去不去健吧，他去的话我顺路也去了算了。
* 王道，🐙已到，准备猛攻！
