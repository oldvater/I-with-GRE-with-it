# 2026.2.19
淦，昨天被空调外机支架擦伤了，赶紧去打了破伤风，回来都没心气写日记了，只能今天补。
回双峰了。

## 力扣每日一题:
416.分割等和子集
  [https://leetcode.cn/problems/partition-equal-subset-sum?envType=study-plan-v2&envId=top-100-liked]

## 思路：
本质是去找元素，构成和为sum/2的数组即可（sum必须为偶数，奇数直接判false），本质是01背包问题
。
```python
  class Solution(object):
    def lengthOfLIS(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        cache = [-1] * (n+1)

        def dfs(i):
            res = 0
            if cache[i] != -1:
                return cache[i]
            for j in range(i):
                if nums[j] < nums[i]:
                    res = max(res, dfs(j))
            cache[i] = res + 1
            return res + 1
        
        ans = 0
        for i in range(n):
            ans = max(ans, dfs(i))
        return ans
        
```


## 每日碎碎念
* 淦，昨天被创了，赶紧去打了破伤风。
* 蒜鸟蒜鸟，就当今年的血光之灾已经度过了，接下来就是一帆风顺。
