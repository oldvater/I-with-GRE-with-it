# 2026.3.11
wc，ww考了454，吓哭了。
越学越爽，状态越学越来，难道我真的是天赐11408选手？



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 1/6 | 5 | 开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[二分查找]
- **周期剩余**：5 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 704.二分查找
  [https://leetcode.cn/problems/binary-search]

## 思路：
- 经典二分查找，找到直接返回，找不到就根据nums[mid]和target的大小调整查找区间。
```python
  class Solution(object):
    def search(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        n = len(nums)
        left, right = 0, n - 1
        while left <= right:
            mid = (left + right)/2
            if nums[mid] == target:
                return mid
            elif nums[mid] < target:
                left = mid + 1
            else:
                right = mid - 1
        return -1 
```
- 744.寻找比目标字母大的最小字母
  [https://leetcode.cn/problems/find-smallest-letter-greater-than-target]

## 思路：
- 用一种叫做染色法的方法，简单来说就是寻找小于等于target的数，这样子最后的right就是大于target的数在数组里的位置
  （也就是尽可能往后新加入一个target加入到数组里维持有序数组的位置），通过染色法找到target应该出现的位置，
  这样这个位置就一定是大于target的最小字母，如果right = n的话，说明不存在比target大的字母，要加的话也只是往数组最后新加，
  故返回letters[0]。
```python
  class Solution(object):
    def nextGreatestLetter(self, letters, target):
        """
        :type letters: List[str]
        :type target: str
        :rtype: str
        """
        n = len(letters)
        left, right = -1, n
        while left + 1 < right:
            mid = (left + right) // 2
            if letters[mid] > target:
                right = mid
            else:
                left = mid
        
        return letters[right] if right < n else letters[0]
```
- 2529.正整数和负整数的最大计数
  [https://leetcode.cn/problems/maximum-count-of-positive-integer-and-negative-integer]

## 思路：
- 通过染色法找到0所在的位置，然后调整left和right直到它们对应的值不为0（在[0,n-1]范围内），
  这样，负整数的个数就是left+1，正整数的个数就是n-right，取最大值即可。
```python
  class Solution(object):
    def maximumCount(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        left, right = -1, n
        target = 0
        while left + 1 < right:
            mid = (left + right)//2
            if nums[mid] > target:
                right = mid
            else:
                left = mid
        while left >= 0 and nums[left] == 0:
            left -= 1
        while right <= n - 1 and nums[right] == 0:
            right += 1
        return max(left + 1, n - right)
```
## 考研每日进度
- 408：栈和队列以及其应用
- 数学：第一讲1000题对应基础部分。

## 每日碎碎念
* wc，越学越爽，越学状态越来，难道说？
* 有一说一，高中我也是这样，对于计算要求高的物理就不怎么算得明白，但对记忆和思维要求高的化学生物和数学就挺有天赋，
  或许这就是我11408成王的理由（bushi）。
* 明天开数学第二讲，顺便把数据结构第三章完成，不要成为进度哥和耐听王，规律要总结，代码要背，题要写。
* 我干，还是得早点去健，今天就因为晚了半小时在健身房又被硬控半小时，合计浪费了一小时，明天还是得早点起。
* ww神了，控制考了454，吓哭了，nb，希望我明年也能考个400分吧，ε=(´ο｀*)))唉。
