# 2026.2.26
果然目前最舒适的速度是两天一章。

## 力扣每日一题:
72.编辑距离
  [https://leetcode.cn/problems/edit-distance?envType=study-plan-v2&envId=top-100-liked]

## 思路：
做这道题，首先得把三种操作翻译成递推的文字：插入-dfs（i，j-1）（对于“ab”和“abc”，i，j都指向最后一位，那么ab添加字母就相当于直接跳过c，j-1）、
修改-dfs（i-1，j-1）（对于“ab”和“ac”，修改就相当于默认i和j的元素已经相等了，直接往前即可，i-1，j-1）、
删除-dfs（i-1，j）（对于“abc”和“ab”，删除相当于跳过c，i往前移，i-1），知道了三个操作的实现逻辑后，
就可以得到递推式：如果word1[i] == word2[j]，直接都往前即可，即dfs（i，j）= dfs（i-1，j-1），
如果不相等的话，说明需要进行上述三种操作中的一个，因此，dfs（i，j） = min（dfs（i-1，j），dfs（i，j-1），dfs（i-1，j-1））+1。
同时要注意边界条件：当i<0,说明i已经遍历完了，此刻，如果j>=0,说明word2还没遍历完，这时候说明word1长度少了，需要插入，插入的数量为j+1
同理，如果j<0时，i>=0，说明word1长了，需要删除i+1个元素。最后加入记忆化存储即可。
```python
  class Solution(object):
    def minDistance(self, word1, word2):
        """
        :type word1: str
        :type word2: str
        :rtype: int
        """
        m, n = len(word1), len(word2)
        cache = [[-1]*n for _ in range(m)]
        def dfs(i, j):
            if i < 0 or j < 0:
                return j + 1 if i < 0 else i + 1
            if cache[i][j] != -1:
                return cache[i][j]
            if word1[i] == word2[j]:
                res = dfs(i-1, j-1)
            else:
                res = min(dfs(i-1, j), dfs(i, j-1), dfs(i-1, j-1)) + 1
            cache[i][j] = res
            return res
        
        return dfs(m-1, n-1)
        
```

## 每日碎碎念
* 一天半章，照这个速度，3月3号能够看完，3月5号出发，刚刚好。
* 晚上玩了2小时瓦，难道这游戏真的很好玩？
* 有一说一，看了taalas那个大模型芯片的报道，我感觉这个挺有潜力的，因为将本地需要调用算力的大模型直接硬件化了，
  应该会在那种工业领域上用的多。
