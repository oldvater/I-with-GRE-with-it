# 2026.3.31
完美完成！看我力挽狂澜！



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 7/8 | 1 | 🔄 挂起 | |
| 5 | 链表 | 14  | 6/14 | 8 | 开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 4)
- **目标单元**：[链表]
- **周期剩余**：1 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 147.对链表进行插入排序
  [https://leetcode.cn/problems/insertion-sort-list]

## 思路：
- 吼吼，刚学完这就来一个习题加深记忆的吼，不过还是乱了，最后靠ai才捋清。
- 维护一个有序边界last_sorted，对于当前节点，即last_sorted的next节点，如果值大于等于last_sorted的值的话，就不需要插入了，last_sorted
  直接后移即可，如果小于的话，就从头开始遍历，因为可能是全局最小，导致头结点后移，因此要加入哨兵节点，从哨兵节点开始往后移动，
  直到prev.next的值大于当前值为止，这说明当前节点需要插在prev的后面，然后就是插入环节，last_sorted的next指向当前节点的next，
  然后当前节点的next指向prev的next，prev的next指向当前节点即可，记得移动当前节点指针到last_sorted的next。最后返回dummy.next即可。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def insertionSortList(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        if not head or not head.next:
            return head

        dummy = ListNode(0)
        dummy.next = head

        lasted_sort = head
        curr = head.next

        while curr:
            if lasted_sort.val <= curr.val:
                lasted_sort, curr = curr, curr.next
                continue
            
            prev = dummy
            while prev.next.val <= curr.val:
                prev = prev.next
            lasted_sort.next = curr.next
            curr.next = prev.next
            prev.next = curr

            curr = lasted_sort.next
        return dummy.next

```

## 考研每日总结
- 408：数据结构完成，再见了，数小弟，7月再见，骗你的，明天估计还得复盘一下整本书。
- 数学：第九讲1000题写完，正确率大约只有3/4，明天勘误一下，不过起码抢险成功了。

## 每日碎碎念
* 看我力挽狂澜！3月任务圆满完成！进入4月阶段。
* 为了完成这个我今天其他什么都没搞，麻了，不过好在结局是好的，最终完成了。
