# 2026.3.27
麻了，就这样吧，周日出去放松一下，看能不能缓解一下状态。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 7/8 | 1 | 🔄 挂起 | |
| 5 | 链表 | 14  | 3/14 | 11 | 开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 4)
- **目标单元**：[链表]
- **周期剩余**：4 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 203.移除链表元素
  [https://leetcode.cn/problems/remove-linked-list-elements]

## 思路：
- 这题很好地回答了灵神的第一个问题：**什么时候用哨兵节点**。当需要变动头节点的时候，需要引入哨兵节点，包括但不限于删除、翻转。
- 这题的思路就很普通，同时维护pre和cur，pre为cur的前驱节点，当cur的值为目标值时，删除节点，表现为pre的next变为cur的next
  （此时pre仅调整，不移动，因为调整后pre指向的是移动后的cur，视为pre进行了移动），如果不为目标值的话，pre就要往后移动，
  最后cur往后移动。由于头结点可能被删除，因此引入哨兵节点，pre初始化为哨兵节点，最后返回哨兵节点的next就行。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def removeElements(self, head, val):
        """
        :type head: Optional[ListNode]
        :type val: int
        :rtype: Optional[ListNode]
        """
        pre = ListNode()
        dummy = pre
        pre.next = head
        cur = head
        while cur is not None:
            if cur.val == val:
                pre.next = cur.next
            else:    
                pre = cur
            cur = cur.next
        return dummy.next
```
- 3217.从链表中移除在数组中存在的节点
  [https://leetcode.cn/problems/delete-nodes-from-linked-list-present-in-array]

## 思路：
- 这题和上一题一样，删除点的操作类似，不过判断是否删除的逻辑变为将nums转为集合，然后判断cur的值是否在集合里，在的话就删除，
  最后返回哨兵节点的next即可。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def modifiedList(self, nums, head):
        """
        :type nums: List[int]
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        s = set(nums)
        pre = dummy = ListNode()
        pre.next = head
        cur = head
        while cur is not None:
            if cur.val in s:
                pre.next = cur.next
            else:
                pre = pre.next
            cur = cur.next
        return dummy.next
        
```
- 83.删除排序链表中的重复元素
  [https://leetcode.cn/problems/remove-duplicates-from-sorted-list]

## 思路：
- 这题本质上也是删除节点的题，维护一个集合，记录目前出现过的节点，对于cur，如果cur的值在集合中，说明已经出现过，删除，
  否则将cur的值加入集合中，最后返回哨兵节点的next。
- 这题其实不需要哨兵节点的，因为第一个节点不可能被删除（第一之前不存在节点，何谈重复），但是为了写着舒服，还是引入了哨兵节点。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def deleteDuplicates(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        s = set()
        pre = dummy = ListNode()
        pre.next = head
        cur = head
        while cur is not None:
            if cur.val not in s:
                s.add(cur.val)
                pre = pre.next
            else:
                pre.next = cur.next
            cur = cur.next
        return head
```

## 考研每日总结
- 408：B树和B+树。
- 数学：第八讲1000题完成。

## 每日碎碎念
* 绝望了，看明天408能不能写完7.2吧。
* 自己写的fastapi接口竟然没有太多的错误！rag正式接入pdf导入系统！nb
