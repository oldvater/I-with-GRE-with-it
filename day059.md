# 2026.2.21
最艰难的部分已经搞完了，接下来就是收尾工作了。

## 力扣每日一题:
32.最长有效括号
  [https://leetcode.cn/problems/longest-valid-parentheses?envType=study-plan-v2&envId=top-100-liked]

## 思路：
我想起来了之前也有一道判断有效括号范围的题？好像是用栈做的。
这题也可以用栈来做，首先，遇到“(”就把对应的下标压入栈，而遇到了")"时，如果之前压入过"("，那么就弹出对应的左括号
，如果没有压入，那么这个“）”就不可能组成有效括号了，连带着之前的所有“）”都没有意义，因此将起点更新为当前下标，
计算长度的时候用有效“）”减去最近的红线，得到的值维护答案最大值即可（初始红线为-1，因此stk[0]=-1）。
```python
  class Solution(object):
    def longestValidParentheses(self, s):
        """
        :type s: str
        :rtype: int
        """
        stk = [-1]
        ans = 0
        for i, ch in enumerate(s):
            if ch == '(':
                stk.append(i)
            elif len(stk) > 1:
                stk.pop()
                ans = max(ans, i - stk[-1])
            else:
                stk[0] = i
        return ans
        
```


## 每日碎碎念
* 这么一看，两天就能看一章呀，爽。
* 买到票了，还有14天就出发去学校了，寒假这么快就结束了吗（悲）。
