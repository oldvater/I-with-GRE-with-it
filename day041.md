# 2026.2.1
晚上在五一广场玩，爽。

## 力扣每日一题:
  394.字符串解码
  [https://leetcode.cn/problems/decode-string?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 递归：默认函数的功能是找到当前头元素，如果是字母，就返回字母加上下一字符开头的切片,
  如果是数字，那么找到后续第一个‘[’，位置记为i，记录balance=1，后续，遇到‘[’,balance+1,
  遇到‘]’,balance-1,当balance=0时，说明遇到了与第一个左括号匹配的右括号。位置记为j，
  那么从0到i中间所有元素为数字，为int(s[:i]).要连续的字符串为s[i+1:j],
  返回s[i+1:j]*int(s[:i]) + self.decode(s[j+1:])。
  ```python
  class Solution:
    def decodeString(self, s):
        if not s:
            return s

        if s[0].isalpha():
            return s[0] + self.decodeString(s[1:])

        i = s.find('[')
        balance = 1
  
        for j in count(i + 1):
            if s[j] == '[':
                balance += 1
            elif s[j] == ']':
                balance -= 1
                if balance == 0:
                    return self.decodeString(s[i + 1: j]) * int(s[:i]) + self.decodeString(s[j + 1:])
- 迭代：一个一个遍历，当遇到字母的时候直接往里面加入，遇到数字就更新k为k*10+数字，遇到左括号，就加上k * decode（）
  （定义迭代数i为全局变量，这样迭代子数组就会位于左括号的位置，在迭代初始+1即可）在运算完以后k清零，
  以免类似3[a]2[b]中k被记为32，遇到右括号则退出，这样，每遇到一次左括号，就进一层函数，遇到一次右括号，就退出，
  最后刚好完成当前函数层。
  ```python
  class Solution(object):
    def __init__(self):
        self.i = 0
    def decodeString(self, s):
        """
        :type s: str
        :rtype: str
        """

        def decode():
            res = ''
            k = 0
            while self.i < len(s):
                c = s[self.i]
                self.i += 1
                if c.isalpha():
                    res += c
                elif c.isdigit():
                    k = k*10 + int(c)
                elif c == '[':
                    res += decode() * k
                    k = 0
                else:
                    break
            return res
        
        return decode() 


## 每日碎碎念
* 有一说一，初一从有理数转到代数学习确实比较难理解，辅导初一有感。
* 五一广场还行，那个大烤串和炸虾不太好吃
