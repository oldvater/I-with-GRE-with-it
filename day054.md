# 2026.2.14
明天15，后天16，年头将近。

## 力扣每日一题:
279.完全平方数
  [https://leetcode.cn/problems/perfect-squares?envType=study-plan-v2&envId=top-100-liked]

## 思路：
做题以来最难的题之一，本质是在nums=[1,4,...,i^2]的数组中找到组成n的最少数量，实质上相当于价值相等的完全背包
当i为0时，意味着1这个最小单元已经结束，递归达到边界，这时候如果j=0，则返回0，j不等于0则不合法，记为inf，
当j小于i^2时，意味着i的平方大于j，要下调一级，如果j大于i^2，说明i可以能被取到，因此要在（i-1，j）(不取)和（i，j-i^2）+1
(取)中进行最小值判断，同时记录在对应的二维数组cache中，来优化内存（cache的第一层是i，因此最大值是n^0.5,第二层是j,因此最大值是n）。
```python
  class Solution(object):
    def numSquares(self, n):
        """
        :type n: int
        :rtype: int
        """
        n1 = int(n**0.5)
        cache = [[-1]*(n+1) for i in range(n1+1)]
        def dfs(i, j):
            if i == 0:
                return float('inf') if j else 0

            if cache[i][j] != -1:
                return cache[i][j]
            
            if j < i**2:
                res =  dfs(i-1, j)
            else:
                res = min(dfs(i-1,j), dfs(i, j - i**2) + 1)
            cache[i][j] = res
            return res


        return dfs(n1, n)
        
```


## 每日碎碎念
* 这道题可以通过数论做出来，气笑了。
* 今天虽然是情人节，但是什么都没做，依旧无聊的一天，逛也没去逛。
