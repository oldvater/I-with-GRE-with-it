# 2026.2.27
现在ai真nb啊。

## 力扣每日一题:
136.只出现一次的数字
  [https://leetcode.cn/problems/single-number?envType=study-plan-v2&envId=top-100-liked]

## 思路：
- 最开始的想法就是先建立一个元组，然后对元组里的元素进行遍历搜索counter，为1的就是答案。
```python
  class Solution(object):
    def singleNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        l = tuple(nums)
        for x in l:
            if nums.count(x) == 1:
                return x
        
```

-  然后灵神告诉我这题可以直接用异或来做，两个相同的数异或为0，而只有一个的数与0异或还是自己。
```python
  class Solution(object):
    def singleNumber(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        return reduce(xor, nums)
        
```

## 每日碎碎念
* 长沙飞哈尔滨为什么这么便宜了，只要560（算机建燃油），那还说啥了，起飞。
* 最后两章，搞完下班。
* 现在ai真nb啊，完全靠ai生成了一个检索ai热点消息并每天报告给我的小脚本，2小时就能实现运行你敢信，太逆天了现在的ai。
