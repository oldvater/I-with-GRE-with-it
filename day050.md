# 2026.2.10
明日方舟更新，60抽全拿了，不赖。

## 力扣每日一题:
763.划分字母区间
  [https://leetcode.cn/problems/partition-labels?envType=study-plan-v2&envId=top-100-liked]

## 思路：
先遍历一遍字符串，存储每个字符的结束位置为字典，然后进行第二次遍历，这次遍历记录每遇到一个初始区间内的字符，
就更新区间末尾end为max（end， last[c]），当i == end时，说明end之前的所有字符的结尾位都在end之前，可以划分为一个区间，
因为是返回长度，因此要加入一个start量，ans加入end - start + 1，同时开始记录新区间，start = i + 1。
```python
  class Solution(object):
    def partitionLabels(self, s):
        """
        :type s: str
        :rtype: List[int]
        """
        last = {}
        ans = []
        for i, c in enumerate(s):
            last[c] = i
        start = end = 0
        for i, c in enumerate(s):
            end = max(end, last[c])
            if i == end:
                ans.append(end - start + 1)
                start = i + 1
        return ans

```


## 每日碎碎念
* 明日方舟新年更新，毕业。
* 明天启程，去冷江。
