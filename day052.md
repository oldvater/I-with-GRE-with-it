# 2026.2.12
网吧打瓦，不赖。

## 力扣每日一题:
118.杨辉三角
  [https://leetcode.cn/problems/pascals-triangle?envType=study-plan-v2&envId=top-100-liked]

## 思路：
有一说一，我感觉这个只有采用数学归纳法是动态规划。
本质是对于行数i，不为0和n的列数j，都有c[i][j] = c[i-1][j-1] + c[i-1][j]，这样写递推式即可，我原本以为要用到递归的。
```python
  class Solution(object):
    def generate(self, numRows):
        """
        :type numRows: int
        :rtype: List[List[int]]
        """
        c = [[1] * (i + 1) for i in range(numRows)]
        for i in range(2, numRows):
            for j in range(1, i):
                c[i][j] = c[i-1][j-1] + c[i-1][j]
        return c
```


## 每日碎碎念
* 快手极速版神了，送太多了，明天就可以拿了。
* 网吧打瓦确实不赖。
