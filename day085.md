# 2026.3.19
卧槽，时间过得好快啊，一瞬间第二周就快结束了，还有两天，看能不能这两天干完图和第六讲吧。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 3/8 | 5 | 开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 3)
- **目标单元**：[数据结构（栈，堆）]
- **周期剩余**：4 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 面试题 16.24.数对和
  [https://leetcode.cn/problems/pairs-with-sum-lcci]

## 思路：
- 这和之前写过的K和数对的最大数目类似，都是需要弹出匹配的数，不过这里要维护的是数对，
  那么将那题的更新最大值改为向答案中加入数对即可。
  判断匹配的逻辑是用字典记录未匹配的数的个数，如果匹配了，则对应数的个数减一，如果没匹配，当前对应的个数加一。
```python
  class Solution(object):
    def pairSums(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[List[int]]
        """
        ans = []
        d = defaultdict(int)
        for num in nums:
            if d[target - num] > 0:
                d[target-num] -= 1
                ans.append([target - num, num])
            else:
                d[num] += 1
        return ans
```
- 219.存在重复元素II
  [https://leetcode.cn/problems/contains-duplicate-ii]

## 思路：
- 这题和之前的类似，不过这题是用一个字典记录数num出现的最近下标，如果检测到num在字典里直接计算i-d[num]的值即可。
```python
  class Solution(object):
    def containsNearbyDuplicate(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: bool
        """
        d = {}
        for i, num in enumerate(nums):
            if num in d and i - d[num] <= k:
                return True
            else:
                d[num] = i
        return False
```
- 2260.必须拿起的最小连续卡牌数
  [https://leetcode.cn/problems/minimum-consecutive-cards-to-pick-up]

## 思路：
- 由于这题是判断最小，但是答案要求无结果的话返回-1，那么最开始直接将答案设为-1肯定不行，因此先将答案设置为无穷，
  最后再加一个判断逻辑判断如果是无穷的话，返回-1即可。那么中间遍历的函数逻辑就是和前面一样的数字匹配了，如果匹配，那么就更新答案为
  min(ans, i-d[x]+1)，同时更新d[x]为当前下标即可。
```python
  class Solution(object):
    def minimumCardPickup(self, cards):
        """
        :type cards: List[int]
        :rtype: int
        """
        ans = float('inf')
        d = {}
        for i, x in enumerate(cards):
            if x in d:
                ans = min(ans, i - d[x] + 1)
            d[x] = i
        if ans < float('inf'):
            return ans
        else:
            return -1
```

## 考研每日总结
- 408：树：完结！图：第一小节。
- 数学：第六讲看了一半。

## 每日碎碎念
* 看看能不能在四月之前结束数据结构和高数上吧。
* 今天看了看那个ccf csp，好贵啊，而我又找不到便宜报名的方法，ε=(´ο｀*)))唉，想了想还是算了。
* 这周过得好快啊，只有两天就结束了，看这两天能不能结束第六讲和图吧。
* 中值定理的证明题本质找茬游戏，找到那个构造函数就特别简单，找不到就难得起飞，还是得靠经验啊。
