# 2026.3.20
结束第六讲和对应的1000题！有一说一，要不是今天花了两小时在那个rag上面，我感觉甚至可以顺便结束第七讲。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 4/8 | 4 | 开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 3)
- **目标单元**：[数据结构（栈，堆）]
- **周期剩余**：3 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2001.可互换矩形的组数
  [https://leetcode.cn/problems/number-of-pairs-of-interchangeable-rectangles]

## 思路：
- 维护一个存放x/y的字典，当width/height在字典里存在的时候，说明可以进行互换，答案加存的值，同时对应存的值加1。
```python
  class Solution(object):
    def interchangeableRectangles(self, rectangles):
        """
        :type rectangles: List[List[int]]
        :rtype: int
        """
        d = {}
        ans = 0
        for x, y in rectangles:
            s = x/float(y)
            if s in d:
                ans += d[s]
                d[s] += 1
            else:
                d[s] = 1
        return ans
        
```
- 2815.数组中的最大数对和
  [https://leetcode.cn/problems/max-pair-sum-in-an-array]

## 思路：
- 首先写一个函数find，功能是找到一个数的最大数位，然后就行标准的维护最大值了，当最大数位在字典里存过的时候，
  答案更新为原答案和x+d[s]的最大值，然后更新d[s]为x和d[s]中的最大值。
```python
  class Solution(object):
    def find(self, num):
        ans = 0
        while num > 0:
            ans = max(ans, num % 10)
            num = num // 10
        return ans
    def maxSum(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        ans = -1
        d = defaultdict(int)
        for x in nums:
            s = self.find(x)
            if d[s] > 0:
                ans = max(ans, x + d[s])
            if x > d[s]:
                d[s] = x
        return ans
```
- 3623.统计梯形的数目I
  [https://leetcode.cn/problems/count-number-of-trapezoids-i]

## 思路：
- 这题有点难度，得想到对于每种边，它们对应梯形的数量就等于它们的数量乘上已有的其他边的数量，加入答案后更新其他边的数量，
  将此边的数量加入已有的边，这样，对于后面遍历的边，就是一一对应的关系了。
- 补一个易错点，10的9次方是1e9，不是10e9，同时还要转整形，不然默认是浮点，很神奇吧。
```python
  class Solution(object):
    def countTrapezoids(self, points):
        """
        :type points: List[List[int]]
        :rtype: int
        """
        mod = int(1e9 + 7)
        cnt = Counter(p[1] for p in points)
        ans = s = 0
        for c in cnt.values():
            k = c * (c-1)//2
            ans += s*k
            s += k
        return (int)(ans % mod)
```

## 考研每日总结
- 408：没学。
- 数学：第六讲结束+1000题写完（未批改）。

## 每日碎碎念
* 明天肝半天，把图肝完，然后晚上爽玩。
* wc，vscode的copilot还是太超标了，靠copilot简单做了一个rag项目的学习，nb。
* 中值定理的题好难想思路啊啊啊啊，刘神说做多了就会了，到时候再看吧。
* 刘神进面了，不赖，又续费三年这一块。
