# 2026.3.28
渐入佳境！我的能力已达巅峰！



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 7/8 | 1 | 🔄 挂起 | |
| 5 | 链表 | 14  | 4/14 | 10 | 开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 4)
- **目标单元**：[链表]
- **周期剩余**：3 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 82.删除排序链表中的重复元素II
  [https://leetcode.cn/problems/remove-duplicates-from-sorted-list-ii]

## 思路：
- 由于这题可能会删除头结点，因此我们引入哨兵节点。
- 首先遍历一遍，求出链表中数的出现情况，记为字典，然后第二次遍历的时候，如果d[h.val]大于1，说明有重复元素，直接删掉就行。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def deleteDuplicates(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        pre = dummy = ListNode()
        pre.next = head
        d = defaultdict(int)
        h = head
        while h is not None:
            d[h.val] += 1
            h = h.next
        h = head
        while h is not None:
            if d[h.val] > 1:
                pre.next = h.next
            else:
                pre = pre.next
            h = h.next
        return dummy.next
```
- 237.删除链表中的节点
  [https://leetcode.cn/problems/delete-node-in-a-linked-list]

## 思路：
- 有一说一，这一题上来给我吓一跳：***无法访问head***，这不吓人吗，无法访问head我怎么找到node，后来看评论才发现是只能找到node，
  不能从头遍历找到node的头结点，那么就要用到我们的408小知识了，对于非尾节点，可以采用将后一个节点复制到当前节点，然后删除后一节点，
  达到删除当前节点的目的。因此我们将后一节点的值覆盖到当前节点，后面节点的next连到当前节点就行。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, x):
#         self.val = x
#         self.next = None

class Solution(object):
    def deleteNode(self, node):
        """
        :type node: ListNode
        :rtype: void Do not return anything, modify node in-place instead.
        """
        node.val = node.next.val
        node.next = node.next.next
```
- 1669.合并两个链表
  [https://leetcode.cn/problems/merge-in-between-linked-lists]

## 思路：
- 这题只需要维护一个计数值，第一次遍历找到切割点，当计数值等于a-1和b+1的时候分别存储对应的节点，这就是切割点，
  然后直接切割，a-1的节点的next直接连到list2，然后往后遍历，到list2的尾部时，将next连到b+1对应的节点即可。
- 怀疑可能搞出来a=0的逆天操作，因此直接加一手哨兵节点，但这样的话从哨兵节点出发的初始计数为-1。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def mergeInBetween(self, list1, a, b, list2):
        """
        :type list1: ListNode
        :type a: int
        :type b: int
        :type list2: ListNode
        :rtype: ListNode
        """
        cnt = -1
        pre = dummy = ListNode()
        dummy.next = list1
        while pre is not None:
            if cnt == a - 1:
                left = pre
            if cnt == b + 1:
                right = pre
                break
            cnt += 1
            pre = pre.next
        left.next = list2
        h = list2
        while h.next is not None:
            h = h.next
        h.next = right
        return dummy.next
```

## 考研每日总结
- 408：查找完毕，8.2完成。
- 数学：第九讲课看一半了。

## 每日碎碎念
* 爽，昨天的任务今天圆满完成！进入状态了。
* 学吧，4月之前应该能完成。然后写完第9讲之后可以花点时间写三大计算了，或者等积分完事？
