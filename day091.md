# 2026.3.26
今天效率还是好低，下午基本上没学，就坐在桌子前发呆，麻了。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 7/8 | 1 | 🔄 挂起 | |
| 5 | 链表 | 14  | 2/14 | 12 | 开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 4)
- **目标单元**：[链表]
- **周期剩余**：5 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2181.合并零之间的节点
  [https://leetcode.cn/problems/merge-nodes-in-between-zeros]

## 思路：
- 本来最开始是想着合并成数组然后返回的，没想到这答案还要求输出格式为链表，麻了。
- 遍历链表，对于当前位置，当t的值不等于0的时候一直往后遍历，同时累加期间遍历的所有值，当遍历点为0的时候，那么下一个点一定是非零点，
  将当前点的值修改为记录的累加值，将当前值的next设置为遍历点的next，进入下一个非零区域，继续重复这一步骤，
  最后得到的就是不包含0的合并节点，最后记得返回head.next，因为首节点一定为0。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def mergeNodes(self, head):
        """
        :type head: Optional[ListNode]
        :rtype: Optional[ListNode]
        """
        h = head
        while h is not None:
            tmp = 0
            t = h
            while t.val != 0:
                tmp += t.val
                t = t.next
            if t != h:
                h.val = tmp
                h.next = t.next
            h = h.next
        return head.next
```
- 725.分隔链表
  [https://leetcode.cn/problems/split-linked-list-in-parts]

## 思路：
- 首先要获取链表长度n，获取到以后，将n//k,得到每个区域平分的话至少要有多少节点，然后记录i=n%k，表示多的不能平分的节点，
  这些节点会均匀地出现在前i个切片中，然后对链表进行遍历，先判断i是否有富余（即i是否>0,>0的话说明当前切片需要多一个，
  否则就是后面的区域，维持最低平均水平）获取当前区域节点个数cnt，然后往后遍历cnt-1次（因为当前节点需要算上），停下来之后，
  这个地方就是当前区域的结尾，令h等于结尾的next，即下个区域的开始，然后切开当前节点，令当前节点的next为none，
  然后将当前区域的开头存入ans中，同时记得i-1，表示这个区域已经多一个节点了。再遍历结束后，判断ans的长度，如果不足k，就往里面加一点null节点，
  即遍历结束后的遍历节点。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def splitListToParts(self, head, k):
        """
        :type head: Optional[ListNode]
        :type k: int
        :rtype: List[Optional[ListNode]]
        """
        h = head
        n = 0
        while h is not None:
            n += 1
            h = h.next
        i = n % k
        cnt = n // k
        ans = []
        h = head
        while h is not None:
            t = h
            cnt1 = cnt + 1 if i > 0 else cnt
            for j in range(cnt1 - 1):
                if t:
                    t = t.next
            a = h
            if t:
                h = t.next
                t.next = None
            ans.append(a)
            i -= 1
        while len(ans) < k:
            ans.append(h)

        return ans
```
- 817.链表组件
  [https://leetcode.cn/problems/split-linked-list-in-parts]

## 思路：
- 首先将nums转成一个集合，这样到时候访问就可以直接查看是否在nums中，设置一个连续判断位cont，然后遍历链表，
  对于h的值在nums的情况，如果此时cont=0，说明当前节点是一个链表组件的第一个值，ans+1，cont=1，如果h的值不在nums里，
  中断当前连续状态，cont=0。
```python
  # Definition for singly-linked list.
# class ListNode(object):
#     def __init__(self, val=0, next=None):
#         self.val = val
#         self.next = next
class Solution(object):
    def numComponents(self, head, nums):
        """
        :type head: Optional[ListNode]
        :type nums: List[int]
        :rtype: int
        """
        t = set(nums)
        ans = 0
        h = head
        cont = 0
        while h is not None:
            if h.val in t and cont == 0:
                ans += 1
                cont = 1
            if h.val not in t:
                cont = 0
            h = h.next
        return ans
```

## 考研每日总结
- 408：红黑树。
- 数学：第八讲看完了，1000明天能写完。

## 每日碎碎念
* 今天下午坐桌子前一下午没有一点进度这一块，服了，两章看了快一周这一块，麻了，最后这几天赶一赶进度吧。
* 红黑树好麻烦啊，但其实让写伪代码的话也没有那么麻烦。
* 第八讲真难啊，那个极限收敛性好难判断啊，没办法，只能后期多练。
