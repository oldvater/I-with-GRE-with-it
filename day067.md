# 2026.3.1
孩子们，还有最后一章，不行还不能笑，就在后天，和hot100一起结束战斗吧。

## 力扣每日一题:
75.颜色分类
  [https://leetcode.cn/problems/sort-colors?envType=study-plan-v2&envId=top-100-liked]

## 思路：
- 依愿定征，鉴定为纯纯的排序，你不让我用sort函数，那我自己写一个快排不就好了吗。
  又回到了快排的思路，先随机找一个i，值记录为pivot，然后把nums[i]和nums[left]交换位置，方便排序。
  令i=left+1,j=right，在这个区间里进行迭代，当nums[i]<pivot时，i+=1,当nums[j]>pivot时，j-=1，
  这样，当后续比较时，nums[i]一定大于pivot，nums[j]一定小于pivot，就可以交换了，当i>=j时退出循环。
  这个时候，nums[j]指向的位置的值一定小于pivot（因为i经过j了,i前进的条件就是nums[i]<pivot）
  将nums[left]和nums[j]互换位置，这样pivot左边的所有元素一定小于等于pivot，右边的元素一定大于等于pivot，
  将左右两个区间作为新参数导入递归即可。（注意边界条件，当left>=right时，就说明不需要再排序了）。
```python
  class Solution(object):
    def partition(self, nums, left, right):
        if left >= right:
            return 
        i = randint(left, right)
        pivot = nums[i]

        nums[i], nums[left] = nums[left], nums[i]
        i, j = left + 1, right

        while True:
            while i <= j and nums[i] < pivot:
                i += 1
            
            while i <= j and nums[j] > pivot:
                j -= 1
            
            if i >= j:
                break
            
            nums[i], nums[j] = nums[j], nums[i]
            i += 1
            j -= 1
        
        nums[j], nums[left] = nums[left], nums[j]
        
        self.partition(nums, left, j - 1)
        self.partition(nums, j + 1, right)

    def sortColors(self, nums):
        """
        :type nums: List[int]
        :rtype: None Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        self.partition(nums, 0, n-1)
        return nums
        
```

-  第二种就是，记录0出现的次数p0,0,1出现的次数p1，这样小于p0的就是0，p0到p1的就是1，大于p1的就是2。
  可以先写当前值为2，然后看记录的值x是多少，<=1的话，把p1位置的值覆盖为1，然后p1加1，
  为0的话把p0位置的值覆盖为0，然后p0加一。这样最后就可以得到排列好的数组。
```python
  class Solution(object):
    def sortColors(self, nums):
        """
        :type nums: List[int]
        :rtype: None Do not return anything, modify nums in-place instead.
        """
        p0 = p1 = 0
        for i, x in enumerate(nums):
            nums[i] = 2
            if x <= 1:
                nums[p1] = 1
                p1 += 1
            if x == 0:
                nums[p0] = 0
                p0 += 1
        return nums
        
```

## 每日碎碎念
* 脚本可以正常运作，nb。
* 加考研群了，起码算是一个很大的进步。
* hot100和稿子同步结束，爽，刷完hot100后，有一说一，在考虑用c语言二刷还是去刷灵神的题单，到时候再说吧。
