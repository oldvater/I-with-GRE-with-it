# 2026.3.2
99题了，明天就都结束了。

## 力扣每日一题:
31.下一个排列
  [https://leetcode.cn/problems/next-permutation?envType=study-plan-v2&envId=top-100-liked]

## 思路：
- 本质是找到这个数的下一个最小的数，该数的元素和此数的元素一致。
  对于12，下一个数就是21；对于132，下一个数就是213；对于123，下一个数就是132.
  发现，对于一个顺序对（类似123，下一个就是将2和3互换，对于1232，下一个最小的数的量级一定是13xx，即原来2的位置变成3），
  只要找到最后的顺序结构（前面的数小于后面的数），那么就意味着这里的值可以有更大的数（如1231，一定有1312>1231;再如
  14323，一定有14332>它）在后面的逆序结构中找到数来交换（保证大于原数），交换的数为后面的逆序结构中大于它的最小的数（如14321，就是将2和1互换，得到24311）
  （保证数量级最小），同时，因为顺序对是一串数字最小的组合，因此交换后需要保证后续的排列为顺序排列（保证后面的值最小）
  （由于原本后面的数为逆序排列，而交换时仅交换的大于其的最小数，因此，交换值依然大于右边小于左边，依然是一个逆序结构），
  这样，前面部分的数不变，中间部分的数是大于其的最小值，后面的数是最小排列，必然得到大于原数的最小值。
  而当原数完全是逆序排列时，由于不存在更大的数，则转换为正序排列，即最小的数。
```python
  class Solution(object):
    def nextPermutation(self, nums):
        """
        :type nums: List[int]
        :rtype: None Do not return anything, modify nums in-place instead.
        """
        n = len(nums)
        i = n - 2
        j = n - 1
        while nums[i] >= nums[i+1] and i >= 0:
            i -= 1

        if i >= 0:
            while nums[j] <= nums[i]:
                j -= 1
            nums[i], nums[j] = nums[j], nums[i]

        left, right = i+1, n-1
        while left <= right:
            nums[left], nums[right] = nums[right], nums[left]
            left += 1
            right -= 1
        return nums
```


## 每日碎碎念
* 99天了，最后一天金身打成，哈哈哈哈。
* 突然发现复试的时候可以写python代码，难道说？
* 那我可以考虑接下来写灵神的题单了。
* 打过界园五结局了，爽，虽然鏖战至1点
