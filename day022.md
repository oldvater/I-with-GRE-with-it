# 2026.1.12
好吧，真抱歉，中间去休假，有两天没写日记

## 力扣每日一题:
  200.岛屿数量
  [https://leetcode.cn/problems/number-of-islands?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 像水漫金山一样，对于“1”，就深度优先遍历所有连续的“1”，改为“2”，这样就避免了重复遍历同一座岛，然后找到几个
  独立的“1”，ans就是多少。
  ```python
  class Solution(object):
    def numIslands(self, grid):
        """
        :type grid: List[List[str]]
        :rtype: int
        """
        ans = 0
        m = len(grid)
        n = len(grid[0])
        def dfs(i, j):
            if i < 0 or i >= m or j < 0 or j >= n or grid[i][j] != '1':
                return 
            grid[i][j] = '2'
            dfs(i - 1, j)
            dfs(i + 1, j)
            dfs(i, j - 1)
            dfs(i, j + 1)

        for i in range(m):
            for j in range(n):
                if grid[i][j] == '1':
                    ans += 1
                    dfs(i, j)
        
        return ans


        

## 每日碎碎念
* 爽玩三天，明天准备复习了
* 淦，回来太早了，大把人没放假，玩也玩不太动，无聊ing
