# 2026.3.13
wc，南ai出复试分了，399排第二，小池子，冷了，感觉压力有点大。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 3/6 | 3 | 开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[二分查找]
- **周期剩余**：3 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2300.咒语与药水的成功对数
  [https://leetcode.cn/problems/successful-pairs-of-spells-and-potions]

## 思路：
- 对于这类两数相乘大于等于某个数的情况，直接用目标数除以其中某个数组的全部数（这里是potions数组），得到大于等于success时
  spells至少是多少，然后对处理后的potions数组进行排序，这样，对于spells中的每个元素，只需要找到严格大于它的元素j，
  j前面的元素都是满足乘potions大于等于success的，写入spells即可。
```python
  class Solution(object):
    def find(self, nums, target):
        n = len(nums)
        left, right = -1, n
        while left + 1 < right:
            mid = (left + right) // 2
            if nums[mid] > target:
                right = mid
            else:
                left = mid
        return right
    def successfulPairs(self, spells, potions, success):
        """
        :type spells: List[int]
        :type potions: List[int]
        :type success: int
        :rtype: List[int]
        """
        for i, x in enumerate(potions):
            potions[i] = success // x if success % x == 0 else success // x + 1
        potions.sort()
        for i, x in enumerate(spells):
            spells[i] = max(self.find(potions, x), 0)
        return spells
        

        
```
- 2080.区间内查询数字的频率
  [https://leetcode.cn/problems/range-frequency-queries]

## 思路：
- 有一说一，最开始我以为是直接截取arr[left:right+1],然后对切片进行排序，然后获取严格大于value的下标right，与严格小于value的下标left，
  right-left-1就是答案，结果，来了一个长度10000的超长数组，不出意料的超时了（排序居然是nlogn吗）。
  于是采取字典pos来记录每个数字出现的下标，然后对于value，在pos[value]的数组里执行上述操作（找left和right），然后right - left - 1即可。
```python
  class RangeFreqQuery(object):

    def __init__(self, arr):
        """
        :type arr: List[int]
        """
        pos = defaultdict(list)
        for i, x in enumerate(arr):
            pos[x].append(i)
        self.pos = pos
    
    def findright(self, nums, target):
        n = len(nums)
        left, right = -1, n
        while left + 1 < right:
            mid = (left + right)//2
            if nums[mid] > target:
                right = mid
            else:
                left = mid
        return right

    def findleft(self, nums, target):
        n = len(nums)
        left, right = -1, n
        while left + 1 < right:
            mid = (left + right)//2
            if nums[mid] < target:
                left = mid
            else:
                right = mid
        return left


    def query(self, left, right, value):
        """
        :type left: int
        :type right: int
        :type value: int
        :rtype: int
        """
        a = self.pos[value]
        return self.findright(a, right) - self.findleft(a, left) - 1



# Your RangeFreqQuery object will be instantiated and called as such:
# obj = RangeFreqQuery(arr)
# param_1 = obj.query(left,right,value)
```
- 3488.距离最小相等元素查询
  [https://leetcode.cn/problems/closest-equal-element-queries]

## 思路：
- 和上题相似的思路，采用字典s来记录每个数字出现的下标，那么对于查询的nums[queries[i]]，就可以去s里面寻找，如果是数组的长度等于1，
  说明不存在重复元素，返回-1，如果大于1，就寻找x在数组中的位置j，这样，最小距离必由x前面的下标或后面的下标给出，然后由于是循环计算距离，
  所以其实是求|pos[（j-1+n）%n] - x|, m - |pos[(j-1+n)%n] - x|, |pos[(j+1+n)%n] - x|, m - |pos[(j+1+n)%n] - x|四个元素的最小值,
  （防止溢出，比如j=0时，j的前一个元素就是n-1，j=n-1时，j的后一个元素就是0）其中m是nums的长度。
- wc，这个最后的表达式好生复杂，我最后还是靠ai修正才补上的，看都看麻了，不过写成数学式后好理解不少。
- 对于这类查找重复元素的题，都可以用一个字典记录元素出现的下标，然后记录的数组是正序的，就可以用二分法之类的来做了。
```python
  class Solution(object):
    def solveQueries(self, nums, queries):
        """
        :type nums: List[int]
        :type queries: List[int]
        :rtype: List[int]
        """
        m = len(nums)
        s = defaultdict(list)
        for i, x in enumerate(nums):
            s[x].append(i)
        for i, x in enumerate(queries):
            pos = s[nums[x]]
            n = len(pos)
            j = bisect.bisect_right(pos, x) - 1
            queries[i] = min(abs(pos[(j-1+n)%n] - pos[j]), m - abs(pos[(j-1+n)%n] - pos[j]),
            abs(pos[(j+1+n)%n] - pos[j]), m - abs(pos[(j+1+n)%n] - pos[j])) if n > 1 else -1
        return queries
```
## 考研每日进度
- 408：无
- 数学：第三讲刚开一半。

## 每日碎碎念
* wc，没办法，今天下午有课，第一节课得去吧，没办法，然后晚上又被人拉去写传感器代码，一直整到9点20，这还学啥啊，干脆直接回寝室了没办法。
* 明天得补进度了。
* 主要还是心态收到影响了，今天南大出排名了，ai学硕399第二，妥妥的冷了，但是智科软件400分进面，这说明11408考400都不一定有书读了，
  还是被这件事影响心态了，一度想要跳车，不过现在倒是想清楚了，等什么时候小池子里挤满了400分的时候，攒出了
  10个不报软微不报大池子就来和你抢复试斩杀线且初复试55分的时候，那个时候我的分只会更高，任他风雨飘摇，我自岿然不动。
