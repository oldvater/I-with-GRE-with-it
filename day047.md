# 2026.2.7
今天学了一点，但学得不多，发现晚上效率特别差，不读书这一块。

## 力扣每日一题:
121.买卖股票的最佳时机
  [https://leetcode.cn/problems/best-time-to-buy-and-sell-stock?envType=study-plan-v2&envId=top-100-liked]

## 思路：
维护一个最小股票值min_price，同时维护利润最大值ans=max（ans, x-min_price）（min_price更新应该在ans更新后，因为不能在一轮同时买卖）
（本例可以，因为就算更新在更新了min_price后本轮的利润为0，不可能被维护到max里），这样就可以得到利润的最大值。
```python
  class Solution(object):
    def maxProfit(self, prices):
        """
        :type prices: List[int]
        :rtype: int
        """
        ans = 0
        min_price = prices[0]
        for x in prices:
            ans = max(ans, x - min_price)
            min_price = min(min_price, x)
        return ans

```


## 每日碎碎念
* max可以写成（a+b+|a-b|/2），等等，这个我是不是发过来着？
* 好好调整，今天起码学了两小时。
* 啊啊啊啊啊好烦啊，不想开会:`(
