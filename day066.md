# 2026.2.28
道爷我成啦！热点机器人可以正常使用，并且会在16点15分给我推送，我等下改个时间即可，哈哈。

## 力扣每日一题:
169.多数元素
  [https://leetcode.cn/problems/majority-element?envType=study-plan-v2&envId=top-100-liked]

## 思路：
- 本质对对碰，开局先指定数组开头元素，当遇到相同数字时体力加一，遇到不同数字时体力减一，
  当体力归零时，元素改为当前元素，因为必定存在一个多数元素，因此最后的元素一定是多数元素
  （因为，多数元素的cnt>n/2,每个假设n个元素全部碰撞完，需要消耗2*cnt>n，因此最后答案一定为多数元素）。
```python
  class Solution(object):
    def majorityElement(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        num = 0
        for x in nums:
            if num == 0:
                ans = x
            if x == ans:
                num += 1
            else:
                num -= 1
            
        return ans
        
```

## 每日碎碎念
* 26考研出分了，真吓哭了，400遍地走，350不如狗，举考了409，真nb啊。
* 凌晨搞的热点接收bot可以正常使用，不过我到时候估计得加上论文热点，现在这个的消息太商业化了。
* 没事，今年考研大爆，明年考研人就少，毕竟今年分太高，容易劝退，再加上考研人数本身就在下滑，优势在我。
* 不过，有一说一，就22408炸了，11408的分数依旧坚挺，其实不是很慌，加油吧。
* 方差检验我***，好多数据啊，这玩意我觉得写一个函数就能直接代替手算了，手算太折磨了，不过也算快结束了，最后30页，
  努把力的话甚至明天就能结束。
