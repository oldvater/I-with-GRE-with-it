# 2026.3.10
30讲第一讲完结！老将军虎威犹在，何惧极限之有？！



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 开始 | |
| 3 | 二分查找 | 6  | 0/6 | 6 | ⏳ 待开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 1)
- **目标单元**：[滑动窗口与双指针]
- **周期剩余**：0 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2958.最多K个重复元素的最长子数组
  [https://leetcode.cn/problems/length-of-longest-subarray-with-at-most-k-frequency]

## 思路：
- 这题怎么这么简单，真的是1535的题吗。
  用字典记录一个元素出现的次数，遍历数组，对于一个值，如果出现的频率大于K，那么就弹出窗口左端元素（左端对应的频率减少，然后右移）
  直到频率小于等于K，维护窗口的最大值（闭区间，所以是i-left+1）。
```python
  class Solution(object):
    def maxSubarrayLength(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
        dict1 = {}
        ans = left = 0
        for i, x in enumerate(nums):
            if x not in dict1:
                dict1[x] = 0
            dict1[x] += 1
            while dict1[x] > k:
                dict1[nums[left]] -= 1
                left += 1
            ans = max(ans, i - left + 1)
        return ans
```
- 2024.考试的最大困扰度
  [https://leetcode.cn/problems/maximize-the-confusion-of-an-exam]

## 思路：
- 对于这类题目（包括上一题）————连续窗口元素出现频率有限制，提出一个概念，叫**容忍度**。
  也就是窗口中对条件的最大容忍情况，如果超过了，就要移动窗口，直到不再超过为止，上一题的容忍度为每个元素的最大出现频率，也就是K。
- 这道题的容忍度，对连续“T”字符来说，就是连续“T”中能容忍的“F”的个数，对于连续“F”字符来说就是连续“F”中能容忍的“T”的个数。
  一旦超过了容忍度，就要移动窗口左端，直到排出多的那个元素为止。因此对x进行判定，如果等于“T”，就进行连续“F”判定，如果等于“F”，就进行“T”判定，
  最后维护容忍度范围内连续“T”和连续“F”长度的最大值即可。
```python
  class Solution(object):
    def maxConsecutiveAnswers(self, answerKey, k):
        """
        :type answerKey: str
        :type k: int
        :rtype: int
        """
        cnt0 = cnt1 = 0
        left0 = left1 = 0
        ans = 0
        for i, x in enumerate(answerKey):
            if x == 'T':
                cnt1 += 1
                while cnt1 > k:
                    if answerKey[left1] == 'T':
                        cnt1 -= 1
                    left1 += 1
            else:
                cnt0 += 1
                while cnt0 > k:
                    if answerKey[left0] == 'F':
                        cnt0 -= 1
                    left0 += 1
            ans = max(ans, i - left0 + 1, i - left1 + 1)
        
        return ans
```
- 1004.最大连续1的个数|||
  [https://leetcode.cn/problems/max-consecutive-ones-iii]

## 思路：
- 这题的容忍度就是连续1中0的个数。对于这类二进制数组，计算次数时只需要加上1-x（连续1中的0）或x（连续0中的1）即可。
  同样，超过容忍度后，窗口左端右移，次数减去对应的值（1-nums[left]（连续1中的0）或nums[left]（连续0的1）），维护长度最大值即可。
```python
  class Solution(object):
    def longestOnes(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
        cnt = left = ans = 0
        for i, x in enumerate(nums):
            cnt += 1 - x
            while cnt > k:
                cnt -= 1 - nums[left]
                left += 1
            ans = max(ans, i - left + 1)
        return ans
```
## 考研每日进度
- 408：没看
- 数学：第一讲完成（1000题基础还没做）。

## 每日碎碎念
* wc，感觉来了，一天直接斩获极限，难道我真的是11408天才？（
* 早上健可行，健完之后上午也能认真。
* 明天开数据结构第三章了，然后再把第一讲的1000题做了，按部就班这一块。
* 有希望啊，7月之前感觉可以一轮完，爽！
* 滑动窗口感觉也来了，可惜明天就轮换了，话说我要不要链表这些考的数据结构用c语言写啊，明天看看效果吧。
