# 2026.3.18
突然发现上课可以背单词，爽！



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 6/6 | 0 | 完成 | |
| 4 | 数据结构（栈，堆） | 8  | 2/8 | 6 | 开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 3)
- **目标单元**：[数据结构（栈，堆）]
- **周期剩余**：5 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 2342.数位和相等数对的最大和
  [https://leetcode.cn/problems/max-sum-of-a-pair-with-equal-sum-of-digits]

## 思路：
- 写一个求数位和函数，然后设置一个字典。枚举右，维护左的思路，对于一个数num，s是它的数位和，如果s在字典里面，说明存在数位和相等的数，
  更新ans为ans和num+字典里存的数的最大值，最后返回。
- 因此，我们可以知道，字典里需要存的值为数位和为s的已遍历数的最大值，所以在更新ans后，还需要加一个更新d[s]的代码，
  表现为取出d[s]的值x，然后d[s] = max(x, num)即可。如果s不在d里面，把num存进d即可。
```python
  class Solution(object):
    def shuweihe(self, num):
        s = 0
        while num >= 10:
            s += num % 10
            num = num // 10
        s += num
        return s
    def maximumSum(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        ans = -1
        d = {}
        for num in nums:
            s = self.shuweihe(num)
            if s in d:
                x = d[s]
                ans = max(ans, num + x)
                d[s] = max(num, x)
            else:
                d[s] = num
        return ans 
        
```
- 1128.等价多米诺骨牌对的数量
  [https://leetcode.cn/problems/number-of-equivalent-domino-pairs]

## 思路：
- 最开始的想法是将每个数组转化为字符串，然后判断字符串是否在字典里，再看字符串的翻转是否在字典里，在的话答案就加一，
  但是，这样子遇到形如[1,1]这样子的组合时，本来在第一次遇见时不应该更新答案的，但是在原字符串判断不在字典里加入字典后，
  在判断翻转字符串的时候，就会判断在字典里，自动更新答案了，而把检索翻转的流程去掉显然不现实，单独加一个判断逻辑又有点找补的意思，
  因此改用将组合标准化，统一成[min,max]的形式，这样子就只要判断一次是否在字典中即可，非常稳定。同时，作为优化，可以将字符串改成
  用数字存，比如min*10+max的形式，节省空间。
```python
  class Solution(object):
    def numEquivDominoPairs(self, dominoes):
        """
        :type dominoes: List[List[int]]
        :rtype: int
        """
        d = {}
        ans = 0
        for x, y in dominoes:
            if x > y:
                x, y = y, x
            s = x * 10 + y
            
            if s in d :
                ans += d[s]
                d[s] += 1
            else:
                d[s] = 1

        return ans
        
```
- 1679.K和数对的最大数目
  [https://leetcode.cn/problems/max-number-of-k-sum-pairs]

## 思路：
- 看到K和，首先得想到用哈希表存储元素，然后判断k-x在不在表里即可。
- 对于这题，由于匹配成功后要删除匹配的元素，因此加入计数，当k-x的数目为0时，说明前面未匹配的数里没有k-x，因此将x加入未匹配的数中，
  代码表现为c[x]+=1，若c[k-x]>0，说明能进行匹配，更新答案加1，将c[k-x]-=1，视为弹出能匹配的一个元素，同时不将当前元素存入表中，视为
  加入后立刻弹出。
```python
  class Solution(object):
    def maxOperations(self, nums, k):
        """
        :type nums: List[int]
        :type k: int
        :rtype: int
        """
        c = defaultdict(int)
        ans = 0
        for x in nums:
            if c[k - x] > 0:
                ans += 1
                c[k-x] -= 1
            else:
                c[x] += 1
        return ans
```

## 考研每日总结
- 408：ds：树：第四小节。
- 数学：第五讲+对应1000题。

## 每日碎碎念
* 先说结论，下午健浪费时间，同时几乎卡死了所有时间，早上上完早八后只有1h的学习时间，完全没用，而下午健以后还要回寝室洗衣服，
  完完全全打乱原有规划，我认为这也是今天效率低的一个重要原因。
* 数学第五讲效率特别低，听了等于没听，正确率只有50%，果然，进度会骗人，正确率不会，明天得把第五讲重新专门看一遍。
* 408感觉进入状态了（每天都这么说（bushi）），只不过碍于数学的低效率，没能完成今天预定的任务：完成树，明天得抽时间完成。
* 数学可以适当放缓，但是质量必须提上，不指望追上哈吉鲍了，太bt了。
* 好好学，不会差的。
