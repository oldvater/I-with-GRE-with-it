# 2026.2.9
明天明日方舟更新了，期待捏。

## 力扣每日一题:
45.跳跃游戏2
  [https://leetcode.cn/problems/jump-game-ii?envType=study-plan-v2&envId=top-100-liked]

## 思路：
维护当前跳跃能够到达的最远距离与目前遍历的最远距离，当遍历范围超过当前跳跃极限的时候，开始下一次跳跃，count+1，
并把当前跳跃的最远距离修改为已记录的最远距离，这样就可以得到最短跳跃次数，因为第一个到达末尾的跳跃必定由前一次跳跃得来。
```python
  class Solution(object):
    def jump(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        count = 0
        mx = 0
        m1 = 0
        if n <= 1:
            return 0
        for i, x in enumerate(nums):
            mx = max(mx, i + x)
            if i == m1:
                count += 1
                m1 = mx
            if m1 >= n - 1:
                return count

```


## 每日碎碎念
* 第四章，堂堂完结！
* 时间过得好快啊，已经2月10号了，还有一个月就开学了（悲）
