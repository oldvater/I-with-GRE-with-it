# 2026.2.13
舅舅结婚，还行。

## 力扣每日一题:
198.打家劫舍
  [https://leetcode.cn/problems/house-robber?envType=study-plan-v2&envId=top-100-liked]

## 思路：
有一说一，打家劫舍感觉怪怪的。
对于房屋i，只有两种选择：偷或者不偷。偷的话，就是nums[i]+dfs(i-2)(因为第i-1个房屋不能偷，所以只需要考虑从i-2开始的最高)
对于不偷的情况，需要考虑i-1开始的最高值。结束条件就是当当前的房子小于0时，就意味着没有房子偷了。然后再加上一些内存优化，
把第i层的最优结果存入cache数组，需要时直接调用。
```python
  class Solution(object):
    def rob(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        n = len(nums)
        cache = [-1] * (n + 1)
        def dfs(i):
            if i < 0:
                return 0
            if cache[i] != -1:
                return cache[i]
            res = max(dfs(i-1), nums[i] + dfs(i-2))
            cache[i] = res
            return res
        
        return dfs(n-1)
        
```


## 每日碎碎念
* 快手极速版神了，现在已经98了。
* 舅舅结婚，不赖。
