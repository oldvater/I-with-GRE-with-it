# 2026.3.14
虽然第三讲和第四章是看完了，但发现才1/4，有点焦虑了。



## 进度看板

| 优先级 | 单元名称 | 总预算 (Hot100题数) | 当前进度 | 剩余天数 | 状态 | 备注 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| 1 | 数组/哈希 | 8  | 0/8 | 8 | ⏳ 待开始 | 完成难度分 ≤1700 的题目，下同 |
| 2 | 滑动窗口与双指针 | 6  | 6/6 | 0 | 完成 | |
| 3 | 二分查找 | 6  | 4/6 | 2 | 开始 | |
| 4 | 数据结构（栈，堆） | 8  | 0/8 | 8 | ⏳ 待开始 | |
| 5 | 链表 | 14  | 0/14 | 14 | ⏳ 待开始 | 重点复习反转/环 |
| 6 | 二叉树 | 15 | 0/15 | 15 | ⏳ 待开始 | 第一轮只做递归 |
| 7 | 图 | 8 | 0/8 | 8 | ⏳ 待开始 | |
| 8 | 回溯 | 8 | 0/8 | 8 | ⏳ 待开始 | 一口气刷完 |
| 9 | 贪心 | 4 | 0/4 | 4 | ⏳ 待开始 | 一口气刷完 |
| 10 | 动态规划 | 15 | 0/15 | 15 | ⏳ 待开始 | 前六章，难度分 ≤2000 的题目 |


### 当前周期 (Week 2)
- **目标单元**：[二分查找]
- **周期剩余**：2 天 

---
*注：超过7天若未完成，将状态改为“🔄 挂起”，填入剩余天数，然后开启下一行单元。*
## 力扣每日一题:
- 981.基于时间的键值存储
  [https://leetcode.cn/problems/time-based-key-value-store]

## 思路：
- 用两个数组分别存储一个键对应的值和时间戳，查找时通过二分查找找到最近时间戳，由于时间戳和对应的值的下标是相同的，
  因此找到时间戳，然后直接通过下标找到对应值即可。
```python
  class TimeMap(object):

    def __init__(self):
        self.v = defaultdict(list)
        self.pos = defaultdict(list)
        

    def set(self, key, value, timestamp):
        """
        :type key: str
        :type value: str
        :type timestamp: int
        :rtype: None
        """
        self.v[key].append(value)
        self.pos[key].append(timestamp)

        

    def get(self, key, timestamp):
        """
        :type key: str
        :type timestamp: int
        :rtype: str
        """
        va = self.v[key]
        pos = self.pos[key]
        j = bisect.bisect_right(pos, timestamp) - 1
        return va[j] if j > -1 else ""
```
- 1287.有序数组中出现次数超过25%的元素
  [https://leetcode.cn/problems/element-appearing-more-than-25-in-sorted-array]

## 思路：
- 直接把arr转换成元组，得到arr里的所有种类，然后遍历元组的元素，在arr中找到严格小于的下标i，和严格大于的下标j，判断j-i+1是否大于n/4即可。
```python
  class Solution(object):
    def findSpecialInteger(self, arr):
        """
        :type arr: List[int]
        :rtype: int
        """
        n = len(arr)
        u = tuple(arr)
        for x in u:
            if bisect.bisect_right(arr, x) - bisect.bisect_left(arr, x) - 1 >= n / 4:
                return x
```

## 考研每日进度
- 408：串
- 数学：第三讲完成+1000题对应部分。

## 每日碎碎念
* 我干，本来以为能两周结束数据结构的，这下得提速了，不提速真学不完了。
* ε=(´ο｀*)))唉，寝室关系。
* 明天放假！做周总结。
