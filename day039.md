# 2026.1.29
跨时空辉夜神了。

## 力扣每日一题:
  4.寻找两个正序数组的中位数
  [https://leetcode.cn/problems/median-of-two-sorted-arrays?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 吓哭了，这第四题怎么已经到了数学证明的范畴了，中位数代表了能够将一个序列平分成两段，因此我们默认两个数组里的
  元素被分进两段中，同时要保证第一组的所有数小于第二组的所有数，也就是max（第一组）<= min(第二组)，设nums1有i个数在
  第一组，则有j=len(nums1)-i个数在第二组，只需要判断max（ai,bj）<=min（ai+1,bj+1）的情况即可，至于具体的我现在还没太搞明白
  二刷再说。
  ```python
  class Solution(object):
    def findMedianSortedArrays(self, nums1, nums2):
        """
        :type nums1: List[int]
        :type nums2: List[int]
        :rtype: float
        """
        if len(nums1) > len(nums2):
            nums1, nums2 = nums2, nums1
        m, n = len(nums1), len(nums2)
        a = [float('-inf')] + nums1 + [float('inf')]
        b = [float('-inf')] + nums2 + [float('inf')]

        i, j = 0, (m + n + 1) // 2
        while True:
            if a[i] <= b[j + 1] and b[j] < a[i + 1]:
                max1 = max(a[i], b[j])
                min2 = min(a[i+1], b[j+1])
                return max1 if (m + n) % 2 == 1 else (max1 + min2)/2.0
            i += 1
            j -= 1


## 每日碎碎念
* 跨时空辉夜姬神了。
