# 2026.1.1
新年快乐！今天复习到根轨迹了，分离点那一块还不太熟练。

## 力扣每日一题:
  108.将有序数组转化为二叉搜索树
  [https://leetcode.cn/problems/convert-sorted-array-to-binary-search-tree?envType=study-plan-v2&envId=top-100-liked]

### 思路：
- 说不上来的感觉，你让我敲迭代我可能还真敲不出来，但是递归给我的感觉就是只需要默认这个函数会做好它的功能，调用就行了，最多写一个停止案例，
  本题就是直接取中点，然后左边的在左子树，右边的在右子树，默认调用函数过后就能得到排好的子树就行。
  ```python
  class Solution(object):
    def sortedArrayToBST(self, nums):
        """
        :type nums: List[int]
        :rtype: Optional[TreeNode]
        """
        if nums == []:
            return None
        mid = len(nums) // 2
        node = TreeNode()
        node.val = nums[mid]
        node.left = self.sortedArrayToBST(nums[:mid])
        node.right = self.sortedArrayToBST(nums[mid + 1:])
        return node

## 每日碎碎念
* 还是抢不到票
* 自控应该能复习完，但是单片机怎么办啊
* 灵神的题单好多啊，刷完hot100之后就可以顺着灵神的题单慢慢刷了
* 慢慢来吧，考完试回家还得学。
